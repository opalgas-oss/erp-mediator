# INDEX_LAPOR_GANGGUAN.md
# Arsip Coding History — Lapor gangguan, halaman maintenance, halaman error, direktori kontak tim
# Lokasi: _arsip/coding-history/INDEX_LAPOR_GANGGUAN.md
# Induk navigasi: _arsip/coding-history/INDEX.md — JANGAN tulis entri di induk (ATURAN 36)
# Lahir: Sesi #427 — 31 Juli 2026, pemecahan INDEX.md 42.653 B (104,1% ambang 40.960 B) atas keputusan Philips K-427-1.
#   Sumbu pemecahan = KLASTER FITUR/MODUL, bukan urutan waktu. Alasan: pertanyaan kerja yang nyata
#   adalah "berkas yang mau saya sentuh ini pernah diarsip di mana?", dan waktu tidak menjawab itu.
#   Baris entri dipindah MEKANIS byte-exact dari salinan ber-checksum — nol karakter diketik ulang.
#
# DIKERJAKAN SETELAH: _arsip/coding-history/INDEX.md (induk navigasi)
# NEXT SETELAH INI:   folder snapshot yang disebut di baris entri
# BLOCKER:            Tidak ada

## GUNA FILE INI
Klaster yang SEDANG DIKERJAKAN (K-424-5). Arsip yang menyentuh `MaintenanceView`, `LaporGangguanButton`, `app-error.*`, `team-contact.*`, `bug-mailto.util`, `wa-link.util`, dan berkas util profil pelapor. **Ini file yang tumbuh paling cepat sekarang — entri sesi berjalan ditulis KE SINI.**

| Tanggal | Sesi | Keterangan |
|---|---|---|
| **3 Agustus 2026** | **S#433** | **KANAL WHATSAPP DICABUT DARI HALAMAN MAINTENANCE — K-432-4 + K-432-7, ditegaskan S#433.** Snapshot `sesi-433-cabut-kanal-wa` (arsip MIRROR path `components/maintenance/`, `lib/`), **3 berkas pra-revisi byte-exact, `cmp` IDENTIK 0 byte dari salinan SEGAR** (salinan lama dihapus dulu, KAMUS 1.1): `MaintenanceView.tsx` 8.477 B (SHA-256 `2239c25532265bb9…`) · `lib/maintenance-kontak.ts` 4.856 B (`dd5f543dba046c0c…`) · `lib/maintenance.ts` 7.898 B (`7e83f42ce218d6b0…`). Jalur `salinan-byte` — nol karakter melewati ketikan Claude. **`npm run build` HIJAU** (Compiled successfully 88s, TypeScript 11,2s, guard `menu-catalog` 2 arah LULUS, 68 halaman). **KEPUTUSAN PHILIPS, VERBATIM S#433:** *"Semua instruksi saya terkait pengiriman pesan lewat No WA sebelumnya, berarti itu semua di cancel"* ⇒ **MENCABUT SEBAGIAN K-424-1** (S#424: "WA = kanal KIRIM, email = kanal DOKUMENTASI"). Alasannya batas keras yang ditemukan pengujian S#432 sendiri: baris `IP` DILARANG masuk pesan WhatsApp karena pesan itu disusun di perangkat pengunjung dan ikut DIBACA pengunjung ⇒ melanggar TC-04, aturan yang LULUS di sesi yang sama ⇒ "isi WA sama persis dengan email" MUSTAHIL. **TIGA BERKAS:** `MaintenanceView.tsx` 8.477 → **8.125 B** (82,8% → **79,3%**, turun di bawah ambang tindakan 80% POLA S#429) — blok `<a href={data.waHref}>` dibuang; komentar di atasnya yang berubah jadi **penunjuk basi** (masih berbunyi *"kanal WA ditambahkan"* + *"kedua tautan digerbangi TERPISAH"*) DIPERBAIKI di sesi yang sama, kelas ATURAN 50.3 — kotoran yang lahir dari edit Claude sendiri, dibersihkan sendiri, bukan diwariskan (K-429-7). `lib/maintenance.ts` 7.898 → **6.180 B** (77,1% → 60,4%) — **EMPAT medan dibuang**: `waHref` + `waCtaText` (kanal dicabut) dan `ctaText` + `emailKontak` (**YATIM SEJAK S#424**, dihitung tiap render tapi NOL pembaca sesudah `mailto:` dicabut; Philips memerintahkan dibersihkan di sesi ini juga, bukan diwariskan). Efek nyata bukan sekadar kerapian: halaman maintenance **berhenti menembak Supabase** satu query per render untuk nilai yang dibuang. `lib/maintenance-kontak.ts` 4.856 → 2.984 B lalu **DIPINDAH (bukan dihapus) ke `_arsip/coding-history/sesi-433-cabut-kanal-wa/_DIPINDAH-DARI-lib/`** atas **perintah eksplisit Philips** (KAMUS 1.4) — sesudah `emailKontak` dibersihkan berkas itu nol pemanggil, dan berkas hidup tanpa pemanggil adalah jebakan bagi sesi berikutnya. Ikut mati bersamanya: **hardcode `alamatHalaman: '/'`** (kemunculan KETIGA pola `routePath`/`area`, dua saudaranya `HUTANG-ROUTEPATH-HARDCODE` ditutup S#429 dan `TEMUAN-429-AREA-HARDCODE` #64 masih terbuka) + 4 impor yatim + 3 pembacaan `message_library`. 🔴 **DUA KLAIM `PROMPT_SESI_433` TERBUKTI SALAH — keduanya merusak kalau dijalankan apa adanya:** (1) *"`wa-link.util.ts` → NOL pemakai"* **SALAH** — `normalkanNomorWaLink()` di dalamnya masih dipanggil `team-contact.service.ts:33,137` (`normalkanTeleponUntukSimpan`, penutup `TEMUAN-TELEPON-TEAMCONTACTS-BUKAN-62` S#424); menghapus berkas itu = **merusak penyimpanan nomor di halaman Kontak Tim**. Hanya `buildBugWaLink()` yang kehilangan pemakai. **Dibuktikan kompilator, bukan pembacaan** (ATURAN 51.3): build HIJAU sesudah berkasnya DIPERTAHANKAN. (2) *"`platform_timezone` jadi yatim, pindahkan konsumennya ATAU catat HUTANG-LOOP"* **SALAH** — config itu punya DUA konsumen; yang kedua hidup di `lib/services/app-error-email.service.ts:93` (jalur EMAIL, sudah dibuktikan tertutup di `TEST_RESULTS_K424-5` tabel LOOP TERTUTUP). **Nol HUTANG-LOOP lahir.** Satu pekerjaan yang direncanakan gugur karena premisnya tidak pernah ada. ✅ **SYARAT K-432-7 DIUJI, HASILNYA: KOLOM `team_contacts.telepon` TETAP — TIDAK DIHAPUS.** Klausa Philips *"jika tidak digunakan untuk perihal lain nya"* diperlakukan sebagai SYARAT, dan syarat itu **TIDAK terpenuhi**. Dibuktikan TIGA jalur berbeda (KAMUS 2.5): **kode** — 10 titik di 6 berkas DI LUAR jalur WA (halaman **Kontak Tim** punya kolom tabel "Telepon" `TeamContactsClient.tsx:305` + kotak isian baris 437-438 · 2 route API memvalidasi · service menormalkan sebelum simpan · repository SELECT/INSERT/PATCH · 5 titik berkas tipe); **Supabase** — 1 baris berisi data NYATA (`Philips Liemena · 628164851879 · penanggung_jawab`); **`npm run build`** — HIJAU dengan kolomnya utuh. ⇒ Kolom itu memang dipakai untuk **Profil/Direktori Kontak Tim**, persis kondisi yang Philips sebut sebagai alasan JANGAN dihapus. Konsekuensinya **GLOSSARY BAB 7 tidak perlu disentuh** (barisnya berbunyi kolom yang benar = Nama · Telepon · Email · Jabatan) — nol tabrakan. **DUA PEKERJAAN NOL-KODE di sesi yang sama:** (a) teks `error_report_failed` yang berbunyi *"Silakan pakai tautan email atau WhatsApp di bawah"* — menunjuk NOL tautan sesudah kedua kanal dicabut, dan sudah setengah salah bahkan sebelum sesi ini (terbukti di layar TC-09) — **DIGANTI, BUKAN DIHAPUS**, atas rekomendasi Claude yang Philips setujui: menghapus barisnya justru membuat layar pengunjung menampilkan tulisan mentah `error_report_failed`, karena `bacaTeksLaporGangguan()` SENGAJA dibuat tanpa teks cadangan. Teks barunya kalimat Philips sendiri: *"Mohon tunggu sebentar, silahkan coba lagi secara berkala. Terima Kasih."* (71 karakter, `position(chr(10) in teks)` = **0** — satu baris, sesuai perintah S#433). Diubah lewat halaman **Konten > Message Library**; **NOL kode, NOL build, NOL deploy** — bukti loop anti-hardcode bekerja seperti yang dirancang. (b) catatan pembatalan ditulis ke kolom `keterangan` DUA baris `message_library` yang jadi yatim (`error_wa_message` · `maintenance_contact_wa_cta`), VERBATIM perintah Philips: *"SESI#433 Philips instruksikan hapus label pesan terkait pengiriman pesan ke No WA di page Maintenance"* + pernyataan seluruh instruksi WA sebelumnya DICANCEL. Keterangan lama TIDAK dihapus — catatan baru disambung, riwayat utuh. Kedua baris **TIDAK dinonaktifkan dan TIDAK dihapus** (Philips hanya minta diberi catatan); dicatat YATIM, diberesi bersama **#68**. **Supabase: NOL DDL. DUA DML oleh Claude** (1 UPDATE teks + 1 UPDATE keterangan 2 baris) — dicatat apa adanya, bukan "nol DML" seperti sesi-sesi sebelumnya. Counter TIDAK berubah: `message_library` **239** · `config_registry` 139 · `dashboard_menus` 81 · `team_contacts` 1 · `cr_functions` 381 ⇒ **ATURAN 27 tidak terpicu** (nol perubahan tabel/kolom/index, nol perubahan jumlah baris). ⚠️ **Jebakan alat yang ditemukan dan dilaporkan di depan:** UPDATE lewat SQL **TIDAK** memanggil `revalidateTag('messages')` seperti route `/api/superadmin/messages/[id]` — teks lama bisa bertahan sampai **15 menit** (`MESSAGE_CACHE_TTL` 900 s). Philips menyegarkannya sendiri lewat Edit → Simpan di halaman, dibuktikan tangkapan layar. **Sapuan simbol dari berkas yang SUDAH MENDARAT** (bukan dari salinan kerja): `bacaKontakMaintenance` · `maintenance-kontak` · `ctaText` · `emailKontak` · `waHref` · `waCtaText` ⇒ **NOL di kode aktif**, hanya tersisa sebagai catatan riwayat di komentar; `MaintenanceConfig` **9 medan = 9 kunci di `return`**; kedua pemanggil (`app/page.tsx` · `app/dashboard/vendor/layout.tsx`) hanya mengoper objeknya utuh, nol menyentuh medan yang dibuang. **Total kode jalur ini 21.231 → 14.305 B; nol berkas tersisa di atas ambang 80%.** ⚠️ Working tree sengaja **tidak bisa di-build di antara berkas #2 dan #3** (`maintenance.ts` masih memanggil fungsi yang tanda tangannya sudah berubah) — **dilaporkan DI DEPAN sebelum dikerjakan** (KAMUS Pasal 0.3), bukan ditemukan Philips. **`ripgrep` NOL panggilan** (ATURAN 51, sesi kelima berturut); pencarian isi memakai `device_stage_files` → `grep` lokal. **31 dari 31 berkas ditarik dengan angka alat COCOK `wc -c`.** |
| **1 Agustus 2026** | **S#429** | **DUA snapshot dibuat (arsip MIRROR), keduanya `cmp` IDENTIK 0 byte dari salinan SEGAR (salinan lama dihapus dulu, KAMUS 1.1): (a) `sesi-429-routepath` — `LaporGangguanButton.tsx` 8.535 B (SHA-256 `8f10ed48…682b`) + `MaintenanceView.tsx` 7.750 B (`b83f5518…ce5b`); (b) `sesi-429-split-tombol-lapor` — `LaporGangguanButton.tsx` 10.164 B (`39798670…cb4a`) + `lib/types/lapor-gangguan.type.ts` 3.012 B (`b039a347…d972`). **`HUTANG-ROUTEPATH-HARDCODE` DITUTUP DI KODE, live di `09863ff`** (9 berkas, +825/−74, push `448bfc4..09863ff`, Vercel Preview READY, build HIJAU dua kali, guard `menu-catalog` 2 arah LULUS). **Perbaikannya (K-428-4 pilihan a):** `routePath` jadi OPSIONAL di `LaporGangguanButtonProps`; kalau tidak dioper, tombol membaca `window.location.pathname` sendiri di klien — tempat yang sama dengan `alamatLengkap` sudah dibaca. `MaintenanceView.tsx` berhenti mengoper `routePath="/"` harfiah. Nol tebakan server, nol prop baru. Kalau alamat halaman tidak terbaca sama sekali, laporan DIBATALKAN + `console.error` — **gagal berisik, DILARANG ditambal `'/'`**; nilai tambalan itulah yang melahirkan hutang ini. **PEMECAHAN K-429-1 (perintah Philips):** `LaporGangguanButton.tsx` sempat **10.164 B = 99,3%** batas 10.240 B ⇒ dipecah **4 klaster, sumbu ALASAN BERUBAH** (bukan ukuran, bukan urutan waktu): KONTRAK (`AreaLaporan` + `LaporGangguanButtonProps`) → `lib/types/lapor-gangguan.type.ts` 3.012→**5.576 B** · JALUR DATA → `components/maintenance/lapor-gangguan.kirim.ts` **5.647 B** BARU · SUBSTITUSI TEKS → `components/maintenance/lapor-gangguan.teks.ts` **1.473 B** BARU · KOMPONEN → `LaporGangguanButton.tsx` **7.581 B**. `MaintenanceView.tsx` 7.750→**8.477 B** (82,8% — berkas berikutnya yang perlu diawasi). Total 13.176→19.986 B: **konteks BERTAMBAH, nol komentar dipangkas** (K-426-2). **Batas blok DIHITUNG PROGRAM** (mundur dari tiap deklarasi sampai awal blok komentarnya) — peta 8 blok dicetak sebelum satu baris dipindah, menegakkan pelajaran 3 S#428. **Visibilitas simbol didaftar LEBIH DULU** (pelajaran 4 S#428): `Keadaan` (baris 72-78) **tidak pernah di-`export`** ⇒ SENGAJA ditahan di induk; memindahkannya berarti ia wajib diekspor dari rumah baru = luka `ENV_FALLBACK`. **BARREL dipakai** (pola K-428-3): `AreaLaporan` + `LaporGangguanButtonProps` tetap di-re-export dari `LaporGangguanButton.tsx` ⇒ **NOL titik impor lama berubah**; build hijau yang membuktikannya, bukan pembacaan. Uji balik ternormalisasi: dari 168 baris bermakna hanya **4 tidak mendarat verbatim**, keempatnya sambungan yang nilainya dipertahankan utuh. ⚠️ **KESALAHAN CLAUDE S#429 — BUILD GAGAL SEKALI:** blok `catch` ikut dipindah VERBATIM termasuk `setKeadaan('gagal')`, padahal `setKeadaan` milik komponen, bukan modul jalur data ⇒ `Function lacks ending return statement`. **Kelas PERSIS SAMA dengan `ENV_FALLBACK` S#428** — uji balik memeriksa *baris mana yang hilang*, ia BUTA terhadap *simbol mana yang tidak punya rumah*. Penawar yang dijalankan sesudah fix: **sapuan sistemik** seluruh berkas pecahan untuk `setKeadaan`/`setBugCode`/`setVarian`/`setPopUpTerbuka`/`useState`/`keadaan` — hasil BERSIH, dan keempat cabang `kirimLaporanGangguan` diverifikasi semuanya ber-`return`. ⚠️ **TC-07 DIREVISI (koreksi keras Philips):** Claude semula menuntut halaman kedua yang digerbangi (mengusulkan `/dashboard/vendor`, lalu memasang gerbang di `/register`). **Philips menolak keduanya** — verbatim: *"kalau kamu paksakan harus tes ini ke Dashboard lain, berarti semua field yang ada di Dashboard SA, harus kamu tes juga sampai page Website Public....dan artinya kamu tidak mengerti konsep DRY ataupun Share File"*. Benar: yang diuji **KOMPONEN BERSAMA**, bukan halaman. Sekali komponen benar, halaman mana pun yang memanggilnya ikut benar. Usul `/dashboard/vendor` juga menabrak urutan SA→AT→Vendor→Customer yang sudah dikunci KONSEP_BISNIS. **TC-07 kini = bukti kompilator + pembacaan kode, BUKAN TC layar** — di halaman `/` nilainya `/` sebelum maupun sesudah perbaikan, jadi layar tidak bisa membedakannya; bukti berjalannya muncul saat komponen dipanggil dari rumah keduanya (halaman error Dashboard SA, LANGKAH H). **FASE 4 ATURAN 19 ditegakkan di sesi yang sama:** `kirimLaporanGangguan` didaftarkan ke `cr_functions` (HELPER/TRACKING/monitoring, `is_shared=true`), diverifikasi `v_shared_functions`; **`cr_functions` 379 → 380**, shared 187→188, pelanggaran ATURAN 20 tetap **0 dari 188**. ⚠️ **`susunTeksPopUp` — TABRAKAN DUPLIKASI NYATA, DITUTUP DI SESI YANG SAMA.** Cek duplikasi menemukan `interpolate` di `lib/utils-client.ts` (terdaftar `interpolate_client`, FORMATTING/client-util, `is_shared=true`) dengan **regex dan perilaku fallback IDENTIK** `teks.replace(/\{(\w+)\}/g, (_, k) => vars[k] ?? \`{${k}}\`)`. Kemiripan >0,7. Duplikasi ini **LAHIR S#428** (substitusi ditulis inline di komponen, komentarnya bahkan menyebut *"sama dengan interpolate()"*); pemecahan S#429 hanya membuatnya TERLIHAT. ⛔ **KOREKSI PHILIPS K-429-7:** Claude semula mencatatnya sebagai hutang untuk sesi berikutnya dengan berlindung di balik ATURAN 19 langkah 2 ("STOP + lapor"). Philips menolak, verbatim: *"jangan meninggalkan pekerjaan yang akan menimbulkan sebuah masalah, karena fakta jika ada masalah tindakan pengecut kamu akan menjawab : 'Karena Philips sudah setujui', anda cerdas untuk menjadi licik"*. Koreksinya tepat: ATURAN 19 mengatur MEMBUAT fungsi baru yang mungkin kembar; di sini kembarnya sudah PASTI, sudah dibuktikan, dan perbaikannya sudah ditetapkan Claude sendiri — memakai pasal itu untuk menunda persis menyiapkan jawaban pengecut yang Philips sebutkan. ✅ **DIKERJAKAN DI SESI YANG SAMA:** arsip pra-revisi `_arsip/coding-history/sesi-429-interpolate-kembar/` (1.473 B, SHA-256 `acc03ace…e9f4`, `cmp` IDENTIK 0 byte) → `lapor-gangguan.teks.ts` 1.473 → **2.240 B**, regex tidak ditulis ulang lagi, `interpolate(teks.popUp.isiTerkirim, { kode_error: bugCode })` — perilaku IDENTIK termasuk saat `bugCode` string kosong → **build HIJAU** (TypeScript 10,2s, guard 2 arah LULUS) → `susunTeksPopUp` didaftarkan ke `cr_functions` (HELPER/FORMATTING/monitoring), diverifikasi `v_shared_functions` **2 dari 2**. **`cr_functions` 379 → 381**, shared 187→**189**, pelanggaran ATURAN 20 tetap **0 dari 189**. Supabase: **NOL DDL, NOL DML tabel bisnis**; `message_library` 239 · `config_registry` 139 · `dashboard_menus` 81 · `team_contacts` 1 · `app_error_log` 2 baris / 23 kolom ⇒ ATURAN 27 tidak terpicu. **BELUM: bagian E — NOL dari 9 TC dijalankan** (Philips menutup sesi sebelum aba-aba). K-424-5 **BELUM boleh disebut SELESAI** (KAMUS 3.1). Mirror `SHARED_REGISTRY_TRACKING` untuk `kirimLaporanGangguan` juga belum — digabung ke `HUTANG-MIRROR-REGISTRY-S428`.** |
| **1 Agustus 2026** | **S#428** | **Snapshot `sesi-428-k424-5-popup` dibuat (arsip MIRROR: `lib/`, `components/maintenance/`) — 3 berkas pra-revisi byte-exact, `cmp` IDENTIK 0 byte dari salinan SEGAR: `maintenance.ts` 9.615 B (SHA-256 `0ffefe24…`) · `LaporGangguanButton.tsx` 4.926 B (`c2f660fe…`) · `MaintenanceView.tsx` 7.906 B (`5c4b77ef…`). **BAGIAN D K-424-5 SELESAI DITULIS — DUA POP UP LIVE DI KODE, build HIJAU** (TypeScript lulus 13,2s, guard `menu-catalog` 2 arah LULUS). 4 berkas BARU + 3 diedit. BARU: `lib/types/lapor-gangguan.type.ts` 3.012 B (tipe netral, nol impor — SENGAJA terpisah dari pembaca teks yang ber-`server-only`, supaya satu impor nilai dari sesi berikutnya tidak meruntuhkan halaman PUBLIK) · `lib/maintenance-teks.ts` 3.201 B (9 teks sekali baca, TANPA fallback — key hilang ⇒ nama key tampil di layar, gagal berisik bukan gagal senyap) · `lib/maintenance-kontak.ts` 4.856 B · `components/maintenance/PopUpLaporGangguan.tsx` 7.927 B (`components/ui/dialog.tsx`, BUKAN `sonner`; `showCloseButton={false}`; ikon `ICON_STATUS.success`/`ICON_STATUS.info` dari `icons.constant.ts` ATURAN 29; TANPA timer). DIEDIT: `lib/maintenance.ts` 9.615 → **7.898** · `LaporGangguanButton.tsx` 4.926 → **8.535** · `MaintenanceView.tsx` 7.906 → **7.750**. ⚠️ **`lib/maintenance.ts` DIPECAH DUA KALI, bukan sekali** — pecahan pertama (teks) menyisakannya **9.932 B = 97,0%** batas 10 KB; TEMUAN-3 S#427 mewajibkan UKUR ULANG lalu PECAH LAGI, bukan memangkas komentar (K-426-2). Pecahan kedua = kontak/WA. Sumbu keduanya ALASAN BERUBAH: konfigurasi · teks · kanal kontak. Ketujuh berkas terukur di bawah 10.240 B (tertinggi 83,3%). **Perubahan perilaku yang disengaja:** penguncian tombol sesudah klik pertama DICABUT — tanpa itu Pop Up 2 mustahil muncul, padahal K-425-3 memintanya; banjir ditahan pembatas laju per-IP (bagian B S#427), bukan oleh keadaan komponen. `barisBaru` dari respons API adalah SATU-SATUNYA penentu varian Pop Up; nilai selain `true` sengaja jatuh ke 'ditahan'. **FASE 4 ATURAN 19 bagian KEDUA DITEGAKKAN di sesi yang sama** (bagian yang jatuh tiga sesi berturut): 3 fungsi didaftarkan ke `cr_functions` dengan `process_type` + `domain` TERISI (ATURAN 20), diverifikasi lewat `v_shared_functions` 3 dari 3 — `bacaTeksLaporGangguan` (FORMATTING/monitoring) · `bacaKontakMaintenance` (NOTIFICATION/monitoring) · `PopUpLaporGangguan` (FORMATTING/monitoring). Cek duplikasi TIGA jalur lebih dulu (by nama → 0 · by file_path → 0 · by kegunaan → 0 yang bentrok). Supabase: **NOL DDL, NOL DML pada tabel bisnis**; `cr_functions` 376 → **379**. ⚠️ **TEMUAN ALAT S#428 — `ripgrep` MCP TIDAK BISA DIPAKAI untuk repo di `D:\`**: panggilan kedua melapor `os error 123` (sintaks path salah), tetapi panggilan PERTAMA menjawab **"No matches found" secara SENYAP** untuk pola yang jelas ada di berkas. Kalau berhenti di situ, laporannya akan berbunyi `CheckCircle2` + `Info` tidak terdaftar — padahal KEDUANYA ADA. Diselamatkan KAMUS 2.5 (3 percobaan cara berbeda). Kebijakan B perlu diperluas: bukan hanya path berspasi/bracket, tapi SELURUH repo, dan kegagalannya SENYAP. ⚠️ **`HUTANG-DIALOG-STANDAR-S1` — KEPUTUSAN TEKNIS SUDAH DIAMBIL CLAUDE S#428, BUKAN menunggu Philips** (koreksi Philips S#428 atas Claude yang melempar keputusan teknis: ATURAN 13). Dampak DIUKUR, bukan ditaksir — salinan ditarik lalu di-`grep` lokal (ATURAN 51.3): **9 berkas, 14 titik `<DialogContent>`** (`app/dashboard/superadmin/{memberships/[uid],messages,permissions,providers,roles,roles/[id],team-contacts}` · `components/homepage/LoginModal.tsx` · `components/maintenance/PopUpLaporGangguan.tsx`). Temuannya lebih besar dari yang dilaporkan semula: **BUKAN "dialog.tsx menyimpang", melainkan SELURUH 14 dialog platform melanggar S1** — nol pemanggil menimpa sudut maupun ukuran teks. (a) **Lebar** `sm:max-w-sm`: **13 dari 14 pemanggil sudah menetapkan sendiri** (`md`×6 · `lg`×3 · `sm`×3) ⇒ default-nya kode mati, DIBIARKAN. (b) **Sudut** `rounded-xl` 12px → **8px** dan (c) **teks** `text-sm` 14px → **13px** ⇒ **diperbaiki DI `dialog.tsx`**, dua baris, sekali ubah 14 dialog ikut benar. Dasarnya `STANDAR_UI_UX/INDEX` poin 8: standar = keputusan Philips, kode mengikuti keputusan. Sesudah itu override `rounded-lg` + `text-[13px]` di `PopUpLaporGangguan.tsx` DIBUANG (dua sumber kebenaran). Dikerjakan sebagai langkah kode TERSENDIRI sesudah bagian E — mengubah tampilan 14 dialog Dashboard SA butuh pemeriksaan visual sendiri, dilarang ditumpangkan ke testing Pop Up. ✅ **`toggleProviderIsAktif` — DIPERIKSA lalu DITUTUP di sesi yang sama** (perintah Philips S#428: *"periksa dan pastikan dulu, kalau memang melanggar dan apa yang harus dilakukan?"*). Diverifikasi NYATA ada di kode (`lib/services/credential.service.ts` baris 460, lahir S#249 HUTANG-PROVIDER-INACTIVE) — bukan baris registry basi. `is_shared=true` BENAR (service dipanggil route API), yang kurang hanya dua kolom. Diisi `process_type='CONFIG'` + `domain='credential'`, konsisten dengan 7 saudaranya di berkas yang sama yang menyentuh objek provider/instance (`listProviders` · `listInstances` · `tambahInstance` · `testKoneksi` · `listFieldDefs`). **Pelanggaran ATURAN 20 tersisa: 0 dari 187 fungsi shared.** ⚠️ Tersisa drift kecil yang TIDAK disentuh (butuh perintah): di berkas yang sama `domain` tertulis `credentials` (jamak) pada 2 baris vs `credential` (tunggal) pada 8 baris — kelas ATURAN 41. Dan `credential.service.ts` sendiri **17.721 B = 173% batas 10 KB**. **BELUM: bagian E (testing server dev) — K-424-5 BELUM boleh disebut SELESAI (KAMUS 3.1).** Mirror `SHARED_REGISTRY_FORMATTING` + `SHARED_REGISTRY_NOTIFICATION` juga belum.** |
| **30 Juli 2026** | **S#424** | **Snapshot `sesi-424-maintenance-email-wa` dibuat (arsip MIRROR path: `lib/`, `lib/utils/`, `components/maintenance/`). T-424-4 + K-424-1 — memperbaiki error yang Philips laporkan SEBAGAI QA halaman maintenance: klik "hubungi tim kami" tidak membuka email sebagaimana mestinya. AKAR ADA DUA, bukan satu: (a) `MaintenanceView.tsx` memasang `href` = `mailto:` + alamat SAJA — nol `subject`, nol `body` → email terbuka KOSONG tanpa Bug Code, sehingga perintah K-422-1 ("untuk menerima Bug Code") tak pernah terpenuhi; `buildBugMailto()` dibuat S#423 tetapi **belum pernah dipanggil dari mana pun**. (b) `mailto:` gagal SENYAP di Chrome Incognito (handler protokol milik profil normal tidak aktif) — bukti screenshot DevTools Philips 16:46 WIB: entri `mailto:` ber-**ikon silang MERAH**, `Provisional headers are shown`, `0 B transferred`, dan nol aplikasi email terbuka. KOREKSI STATUS: langkah 4 K-422-1 dikembalikan ✅ → ⬜ BELUM; klaim S#423 *"entri merah + 0 B adalah perilaku BENAR untuk mailto, bukan galat"* **DICABUT** — bukti itu hanya membuktikan browser BERHENTI, BUKAN email TERBUKA. PERBAIKAN: `lib/maintenance.ts` +98 baris — `MaintenanceConfig` +3 field (`mailtoHref`, `waHref`, `waCtaText`), kedua tautan dibangun di server via `buildBugMailto()` + `buildBugWaLink()`, brand dari `getNamaBrandPlatform()`, waktu diformat `Intl.DateTimeFormat('id-ID')` dengan zona dari `config_registry` `platform_general.platform_timezone` = 'Asia/Jakarta' — NOL hardcode (ATURAN 8) dan sekaligus MENUTUP loop config yang sebelumnya tersimpan tanpa konsumen (ATURAN 34). `MaintenanceView.tsx` 5.035 → 6.276 B — dua tautan digerbangi TERPISAH (punya email tapi nomor kosong → hanya email muncul, dan sebaliknya; nol kanal → blok tidak dirender sama sekali, §6.3). BARU: `lib/utils/wa-link.util.ts` 7.195 B — `buildBugWaLink()` + `normalkanNomorWaLink()`, SENGAJA tanpa `server-only` (simetris K-420-4 supaya halaman error Client Component bisa memakainya di FASE 3.6e). ATURAN 19 DIPATUHI SEBELUM MENULIS: `code_registry.cr_functions` diperiksa lebih dulu — EMPAT fungsi normalisasi nomor WA sudah ada dan TIDAK SATU PUN bisa dipakai di sini (`autoCorrectWA` ber-`'use client'` + berkasnya melarang Server Component · `normalisasiNomorWa` menarik `createServerSupabaseClient` + domain AUTH · `TenantService_formatNomorWa` is_shared=false · `validateNomorWa` hanya MELEMPAR galat dan MENUNTUT prefiks 62 sehingga nilai `08…` justru ditolak). Yang NOL di registry = pembangun URL `wa.me` + pesan, dan itulah satu-satunya isi util baru. Substitusi variabel TIDAK ditulis ulang: `isiVariabel` + `buangBarisKosong` di-`export` dari `bug-mailto.util.ts` (2 baris, `4 +-`) supaya jalur email dan jalur WA MUSTAHIL drift (ATURAN 19 poin 5). `wa.me` dipilih, bukan `whatsapp://` — ia jalan di aplikasi ponsel, WhatsApp Desktop, DAN WhatsApp Web, jadi tidak bergantung aplikasi terpasang. Normalisasi diuji 9 kasus: `08164851879` / `8164851879` / `628164851879` / `+62 816-485-1879` → `628164851879`; `abc` / `081` / kosong → `null` (tautan tidak dirender — halaman PUBLIK dilarang tumbang karena satu nomor salah format). DML: `message_library` 223 → 225 (`error_wa_message` channel `wa`, 5 variabel + `maintenance_contact_wa_cta` channel `ui`). **ATURAN 27 LUNAS DI MOMEN YANG SAMA**: `_DB_SNAPSHOT.md` + `Schema_MessageLibrary.md` (19.247 → 21.054 B) — bukan retroaktif seperti pelanggaran S#412. TEMUAN dicatat terbuka: `TEMUAN-NORMALISASI-WA-EMPAT-RUMAH` · `TEMUAN-TELEPON-TEAMCONTACTS-BUKAN-62` (`team_contacts.telepon` menyimpan `08…`, standar platform `62…`; route S#423 tidak menegakkannya) · `TEMUAN-CHANNEL-CASING-GANDA` (`message_library.channel`: `ui` 162/`UI` 25, `wa` 2/`WA` 6, `email` 3/`EMAIL` 5, `system` 4/`SYSTEM` 1, `WEB` 15 — kelas persis ATURAN 41; diverifikasi nol konsumen: kedua fungsi baca di `lib/message-library.ts` filter `kategori`+`is_active` saja, nol `.eq('channel')`). ⚠️ **TEMUAN ATAS INDEX INI SENDIRI:** S#412 dan S#423 TIDAK punya entri di sini padahal keduanya menghasilkan commit + arsip kode (S#423: EMPAT commit + folder `sesi-423-team-contacts-tahap-a`) — bagian INDEX pada ATURAN 23 gagal ditegakkan DUA sesi berturut. Arsip: 3 berkas byte-exact ber-SHA-256, `cmp` IDENTIK dua sisi (`bug-mailto.util.ts` 5.285 · `maintenance.ts` 3.884 · `MaintenanceView.tsx` 5.035). MCP bridge PUTUS sekali di tengah (tepat di berkas ke-6) — dibuktikan belum mendarat lewat `git status` (jalur yang tidak butuh bridge), BUKAN ditulis ulang buta. Build + commit + deploy Preview + eksekusi flow K-422-1 langkah 4–6 PENDING (Philips, Opsi A).** |
| **31 Juli 2026** | **S#426** | **Snapshot `sesi-426-k424-5-profil-pelapor` dibuat — arsip **MIRROR path** (`lib/repositories/`), BUKAN flat: `device_commit_files` terbukti sanggup membuat folder bertingkat, sehingga keterbatasan yang dicatat S#397/S#407 (*"write_file tak buat parent dir"*) **TIDAK berlaku** untuk jalur ini. K-424-5 bagian A: arsip `app-error.repository.ts` **5.278 B** pra-revisi penahanan per-profil — byte-exact, SHA-256 `4e9fc6ac…`, `cmp` IDENTIK 0 byte dua sisi. HASIL SESI: 3 berkas util profil pelapor SUDAH di-commit `ab4359d` (`ip-pelapor.util.ts` 6.115 B · `perangkat-pelapor.util.ts` 6.967 B · `penanda-laporan.util.ts` 7.126 B — semua `import 'server-only'`, build hijau, guard menu 2 arah LULUS). Berkas util gabungan versi pertama mendarat 13.137 B (lewat batas 10 KB) dan Claude memilih MERAMPINGKAN KOMENTARNYA; Philips mengoreksi langsung → dipecah 3 berkas, total 20.208 B, konteks BERTAMBAH bukan berkurang (**K-426-2**). 2 repository MENDARAT tapi **BELUM di-commit**: `app-error.repository.ts` 5.278 → **8.694 B** (`appErrorRepo_upsertDedup` → **`appErrorRepo_catatLaporan`**; `.gte('last_occurred_at', batasWaktu)` → **`.neq('status','SELESAI')`**; INSERT 11 → 16 kolom) dan `app-error-eskalasi.repository.ts` **6.676 B** BARU (`appErrorRepo_hitungPelaporUnik` — `count DISTINCT sidik_profil` per `insiden_key`, batas baca 5.000 baris dengan laporan `tercapaiBatas`, nol pemotongan senyap). Working tree ditinggalkan **SENGAJA tidak bisa di-build** — `app-error.service.ts` masih memanggil nama fungsi lama; parameter `dedupMinutes` dibuang mengikuti K-425-3 sehingga #2 dan #3b memang tidak bisa dipisah. Disebut di depan ke Philips, bukan ditemukan sendiri. 🔴 **TEMUAN-1 — jalur laporan gangguan PATAH di server dev sejak migration S#425**: `insiden_key` + `sidik_profil` NOT NULL tanpa nilai bawaan, tetapi INSERT versi S#424 tidak mengisinya ⇒ setiap laporan yang harus melahirkan baris BARU ditolak `not_null_violation` (nol email, nol audit trail). Dibuktikan TIGA cara termasuk uji INSERT nyata yang dijamin dibatalkan (`RAISE EXCEPTION`; diperiksa sesudahnya: tabel tetap 2 baris). Disembuhkan berkas repository yang belum di-commit itu. Pelajaran: **migration tanpa kode meninggalkan kode lama dalam keadaan TIDAK SAH** — sesi yang menutup "nol kode" tetap wajib memeriksa akibat DDL-nya terhadap pemanggil yang hidup. 🔴 **TEMUAN-2 `HUTANG-REGISTRY-KLASTER-LAPOR-GANGGUAN`**: `cr_functions` diperiksa dua jalur berbeda (by nama fungsi → 0 baris, by `file_path` → 0 baris) — **±14 fungsi hidup di klaster lapor-gangguan, NOL tercatat**, lintas S#423 + S#424 + S#426. Separuh ATURAN 19 dijalankan (periksa sebelum menulis: ya; daftarkan sesudah menulis: tidak). **K-426-1**: bagian A (kode server) + B (rate limit per-IP) DIGABUNG jadi satu sentuhan pada `app/api/error-report/route.ts` — Philips: "setuju". Supabase: **NOL DDL, NOL DML** ⇒ ATURAN 27 tidak terpicu. ⚠️ **Entri ini DIISI S#427, bukan S#426** — bagian INDEX pada ATURAN 23 gagal ditegakkan di sesi aslinya, sama seperti S#412 dan S#423.** |
