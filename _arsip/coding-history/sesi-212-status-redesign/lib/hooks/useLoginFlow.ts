// lib/hooks/useLoginFlow.ts — ARSIP SEBELUM EDIT sesi-212-status-redesign
// STATUS-REDESIGN S#212: profile.status -> profile.register_status (di muatDataUser)
// Baris yang diubah: const vendorStatus = (profile.status || '').toUpperCase()
//                 -> const vendorStatus = (profile.register_status || '').toUpperCase()
// File asli ada di git history commit 2e01b19
