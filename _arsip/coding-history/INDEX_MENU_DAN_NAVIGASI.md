# INDEX_MENU_DAN_NAVIGASI.md
# Arsip Coding History — Katalog menu, sidebar, dan guard anti-menu-yatim
# Lokasi: _arsip/coding-history/INDEX_MENU_DAN_NAVIGASI.md
# Induk navigasi: _arsip/coding-history/INDEX.md — JANGAN tulis entri di induk (ATURAN 36)
# Lahir: Sesi #427 — 31 Juli 2026, pemecahan INDEX.md 42.653 B (104,1% ambang 40.960 B) atas keputusan Philips K-427-1.
#   Sumbu pemecahan = KLASTER FITUR/MODUL, bukan urutan waktu. Alasan: pertanyaan kerja yang nyata
#   adalah "berkas yang mau saya sentuh ini pernah diarsip di mana?", dan waktu tidak menjawab itu.
#   Baris entri dipindah MEKANIS byte-exact dari salinan ber-checksum — nol karakter diketik ulang.
#
# DIKERJAKAN SETELAH: _arsip/coding-history/INDEX.md (induk navigasi)
# NEXT SETELAH INI:   folder snapshot yang disebut di baris entri
# BLOCKER:            Tidak ada

## GUNA FILE INI
Arsip `HUTANG-MENU-VISIBILITAS-GOVERNANCE` (ATURAN 49): pembuangan filter whitelist, halaman placeholder dari `message_library`, dan guard build-time 2 arah. **Wajib dibuka sebelum menyentuh `SidebarNav.tsx`, `nav.constant.ts`, `menu-route.constant.ts`, atau `menu-catalog.guard.test.ts`** — `prebuild` menjalankan guard itu 2 arah.

| Tanggal | Sesi | Keterangan |
|---|---|---|
| **24 Juli 2026** | **S#407** | **Snapshot `sesi-407-hapus-filter-whitelist-konfigurasi` dibuat (arsip FLAT + _MAP_PATHS.md). LANGKAH 1 HUTANG-MENU-VISIBILITAS-GOVERNANCE (R1, ATURAN 49 / SPEC_TABEL_MENU_KATALOG Bagian 8): hapus filter whitelist grup Konfigurasi di `SidebarNav.tsx` (2 cabang data-driven + fallback) → `subItems = group.items` langsung + buang pemakaian `validFeatureKeys` + import `SA_VALID_FEATURE_KEYS` + prop `featureKeys` dari destructuring (interface TETAP, caller tak disentuh — anti-overreach). `nav.constant.ts`: hapus export `SA_VALID_FEATURE_KEYS` (`SA_NAV_GROUPS` + `SA_KNOWN_MENU_KEYS` TETAP). Efek: grup Konfigurasi SA 5→13 item; 8 sub-config (register_user..sistem) tampil → klik → catch-all placeholder. Visibilitas = is_active semata (K-40). Bukti live: dashboard_menus super_admin sa.konfigurasi 13 anak is_active=true; `SA_VALID_FEATURE_KEYS` dipakai SidebarNav + di-re-export barrel `lib/constants/index.ts` (barrel KETINGGALAN di verifikasi awal — terungkap build S#407 gagal type-check di index.ts:45, lalu re-export dibuang; koreksi jujur ATURAN 47). SidebarNav 12612→11761 B, nav.constant 8306→8013 B, index.ts 2595→2570 B. Arsip: 3 file byte-exact (SidebarNav.tsx 12612, nav.constant.ts 8306, index.ts 2595) + _MAP_PATHS. Build + commit + deploy Preview PENDING (cloud session tanpa terminal — Philips jalankan, Opsi A).** |
| **24 Juli 2026** | **S#407-L2** | **LANGKAH 2 (snapshot sama `sesi-407-hapus-filter-whitelist-konfigurasi`): placeholder dari message_library (anti-hardcode, ATURAN 8/49). INSERT 1 baris message_library: key `placeholder_halaman_pengembangan`, kategori page_ui, channel ui, teks 'Halaman sedang dalam pengembangan' (id d5ebb335). Catch-all `settings/[...slug]/page.tsx` → async server component, baca `getMessage('placeholder_halaman_pengembangan', fallback teks sama)` render {teks} (ganti hardcode 'Konfigurasi untuk modul ini belum tersedia'). Arsip byte-exact via move_file: `settings__slug__page.tsx` 685 B (asal ber-BOM+CRLF); file baru 1127 B (LF). Shared_Database (ATURAN 27): Schema_MessageLibrary +changelog + _DB_SNAPSHOT message_library 149→208 (live count(*), stale sejak S#269). INSERT aditif — Production tak terpengaruh (main belum baca key ini). Build + commit + deploy Preview PENDING (Philips, Opsi A).** |
| **24 Juli 2026** | **S#409** | **Snapshot `sesi-409-guard-menu-catalog-2arah` dibuat (arsip FLAT + _MAP_PATHS.md). LANGKAH 4 (TERAKHIR) HUTANG-MENU-VISIBILITAS-GOVERNANCE = guard build-time 2-arah (Pilihan A, DISETUJUI S#408; SPEC_TABEL_MENU_KATALOG Bagian 8.2). BARU: `lib/constants/menu-route.constant.ts` (fungsi shared `resolveMenuHref` + `SA_SETTINGS_BASE`/`SA_DASHBOARD_ROOT` — satukan duplikasi `navItemToPath`+`resolveHref` lokal) + `lib/guards/menu-catalog.guard.test.ts` (guard Vitest 2-arah live-Supabase, scope super_admin; arah a DB→kode + arah b kode→DB; placeholder settings/[...slug] LOLOS; exempt otomatis route dinamis+landing; env process.env→fallback .env.development.local; DB tak terjangkau/env kosong = GAGAL-MERAH). EDIT: `nav.constant.ts` 8013→6481 B (navItemToPath delegasi resolveMenuHref + BUANG SA_KNOWN_MENU_KEYS/warnUnknownMenuKeys), `SidebarNav.tsx` 11761→11404 B (pakai resolveMenuHref, buang closure resolveHref + import/panggilan warnUnknownMenuKeys), `package.json` 1350→1391 B (script `prebuild: vitest run lib/guards`). Verifikasi hapus simbol export = ANDALKAN BUILD (pelajaran S#407). Bukti mapping live: dashboard_menus super_admin 35 baris vs 26 page.tsx — nol yatim 2 arah (guard HIJAU di state sekarang). Arsip byte-exact: nav.constant.ts 8013, SidebarNav.tsx 11761, package.json 1350 (get_file_info cocok). rec-1 (SHARED_REGISTRY + HUTANG-CR-FUNCTIONS-DB) + build + commit + deploy Preview PENDING (Philips, Opsi A).** |
