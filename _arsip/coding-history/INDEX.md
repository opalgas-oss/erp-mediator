# INDEX — Coding History Archive

**Folder ini menyimpan snapshot file kode SEBELUM perubahan besar/refactor.**

**Tujuan:** Memungkinkan perbandingan "sebelum vs sesudah" refactor, dan rollback manual kalau ternyata refactor menyebabkan regresi.

**Aturan yang melahirkan folder ini:** CODING_RULES_AI — **ATURAN 12: Arsip Coding Sebelum Refactor**

---

## STRUKTUR FOLDER

```
_arsip/
└── coding-history/
    ├── INDEX.md                     ← file ini
    └── sesi-NNN-<label>/             ← snapshot per sesi refactor
        └── <mirror dari struktur project>
```

---

## CARA PAKAI

- Buka file lama di `_arsip/coding-history/sesi-NNN-.../path/ke/file.ts`
- Buka file sekarang di lokasi aslinya
- Pakai VS Code diff (right-click → Compare with Selected)

---

## LOG PERUBAHAN

| Tanggal | Sesi | Perubahan |
|---|---|---|
| 26 Apr 2026 | #057 | Snapshot `sesi-057-baseline` ditambahkan (4 file login flow). |
| 27 Apr 2026 | #058 | Snapshot `sesi-058-langkah-1` + `sesi-058-langkah-2` ditambahkan. |
| 25 Apr 2026 | #062 | Snapshot `sesi-062-hapus-biometric-login` + `sesi-062-vendor-ui-layout` ditambahkan. |
| 27 Apr 2026 | #064 | Snapshot `sesi-064-fix-double-getuser` + `sesi-064-layout-perf` ditambahkan. |
| 27 Apr 2026 | #068 | Snapshot `sesi-068-sendotp-parallel` + `sesi-068-unified-login` ditambahkan. |
| 27 Apr 2026 | #069 | Snapshot `sesi-069-bug013-shared-brandname` ditambahkan. |
| 29 Apr 2026 | #074 | Snapshot `sesi-074-concurrent-session` ditambahkan. |
| 29 Apr 2026 | #075 | Snapshot `sesi-075-perf-parallel-lock` + `sesi-075-custom-access-token-hook` ditambahkan. |
| 29 Apr 2026 | #076 | Snapshot `sesi-076-selesai-login-nonblocking` + `sesi-076-login-coldstart-opt` + `sesi-076-concurrent-banner` ditambahkan. |
| 30 Apr 2026 | #077 | Snapshot `sesi-077-vendor-rsc-jwt-claims` ditambahkan. |
| 1 Mei 2026  | #079 | Snapshot `sesi-079-dry-fix` ditambahkan. |
| 1 Mei 2026  | #080 | Snapshot `sesi-080-keep-warm-dashboard` ditambahkan. |
| 1 Mei 2026  | #081 | Snapshot `sesi-081-rsc-fix` ditambahkan. |
| 2 Mei 2026  | #084 | Snapshot `sesi-084-redis-otp` ditambahkan. |
| 2 Mei 2026  | #085 | Snapshot `sesi-085-redis-otp-fix` + `sesi-085-keep-warm-login` ditambahkan. |
| 4 Mei 2026  | #096 | Snapshot `sesi-096-pl-auth-26` ditambahkan. |
| 4 Mei 2026  | #097 | Snapshot `sesi-097-pl-s08-m1` ditambahkan. |
| 4 Mei 2026  | #098 | Snapshot `sesi-098-pl-s08-m2` ditambahkan. |
| 4 Mei 2026  | #099 | Snapshot `sesi-099-sidebar-opsi-b` ditambahkan. |
| 4 Mei 2026  | #100 | Snapshot `sesi-100-sentralisasi-ui` ditambahkan. |
| 5 Mei 2026  | #101 | Snapshot `sesi-101-dry-fix` ditambahkan. |
| 5 Mei 2026  | #105 | Snapshot `sesi-105-fix-bug-msg-channel` ditambahkan. |
| 5 Mei 2026  | #106 | Snapshot `sesi-106-fix-bug-dialog-tambah` ditambahkan. |
| **7 Mei 2026** | **#109** | **Snapshot `sesi-109-tenant-can-override` ditambahkan (3 file: `route.ts` + `page.tsx` + `ConfigPageClient.tsx` — sebelum refactor akses_ubah → tenant_can_override).** |
| **7 Mei 2026** | **#110** | **Snapshot `sesi-110-spinner-optimasi-config` ditambahkan (`ConfigPageClient.tsx` sebelum: Loader2 spinner + field-level detectHasChanges).** |
| **7 Mei 2026** | **#110** | **Snapshot `sesi-110-fix-isactive` ditambahkan (2 file: `page.tsx` + `ConfigPageClient.tsx` — sebelum: hapus `.eq('is_active', true)` dari query + tambah `enabled` ke detectHasChanges + `is_active` ke handleSave payload).** |
| **8 Mei 2026** | **#114** | **Snapshot `sesi-114-m4-repo-split` ditambahkan (`master-dropdown.repository.ts` 11.4 KB — sebelum dipecah ke `master-dropdown-group.repository.ts` + `master-dropdown-option.repository.ts` per kategori entitas DB. File asli melebihi batas ATURAN 9 (10 KB) — dipecah by entitas (group / option) untuk growth-friendly future operations).** |
| **8 Mei 2026** | **#116** | **Snapshot `sesi-116-m4-sidebar-fix` ditambahkan (3 file: `components/SidebarNav.tsx` + `lib/constants/nav.constant.ts` + `app/dashboard/superadmin/dropdowns/DropdownGroupsTable.tsx`). Sebelum fix: (1) sidebar filter `feature_key` membuang item M4 yang punya halaman sendiri — menu Pilihan Opsi tidak tampil, (2) item `pilihan_opsi` tidak ada `path` override → resolve default ke /settings/pilihan-opsi (404), (3) Fragment shorthand `<>` di dalam `.map()` tidak bisa terima `key` prop → React warning + reconciliation risk. Lahir dari smoke test S#116 yang mengungkap 3 gap dari S#115 yang tidak ketahuan saat build sukses.** |
| **9 Mei 2026** | **#117** | **Snapshot `sesi-117-m4-tab-fix` ditambahkan (1 file: `app/dashboard/superadmin/dropdowns/DropdownGroupsTable.tsx`). Sebelum fix: tab filter kategori implementasi flex buttons — akan overflow saat kategori bertambah (8 kategori aktif + DBF + loyalty + subscription). Fix: ganti jadi `<Select>` dropdown dari shadcn/ui — tidak pernah overflow, scalable. Perubahan: import Select component, hapus div flex+buttons, ganti dengan Select+SelectTrigger+SelectContent+SelectItem.** |
| **10 Mei 2026** | **#122** | **Snapshot `sesi-122-m4-integrasi` ditambahkan (2 file: `DropdownGroupsTable.tsx` + `DropdownOptionsList.tsx`). Sebelum tambah: sort kolom (useSortableTable), tombol Pemetaan (UsageTrackingPanel inline per baris, state per row), tombol Hapus dengan safety check (fetch sp_check_usage → disabled jika TIDAK_AMAN/TIDAK_BISA, confirm banner jika AMAN).** |
| **10 Mei 2026** | **#123** | **Snapshot `sesi-123-usage-panel-t4t5` ditambahkan (1 file: `components/superadmin/UsageTrackingPanel.tsx` 11.9 KB). Sebelum: split file menjadi main + subcomponents + tambah T4/T5 action buttons (Nonaktifkan AKTIF→TIDAK_DIPAKAI, Aktifkan Kembali TIDAK_DIPAKAI→AKTIF). SP sp_check_usage juga diupdate untuk sertakan `id` dependency di output.** |
| **10 Mei 2026** | **#123** | **Snapshot `sesi-123-delete-guard` ditambahkan (2 file: `groups/[id]/route.ts` + `DropdownGroupsTable.tsx`). Sebelum fix: (1) UI isDelBlocked hanya cek TIDAK_AMAN/TIDAK_BISA — verdict=undefined → tombol merah (bug: flash merah dulu sebelum grey), (2) DELETE route tidak ada server-side safety check. Fix: isDelBlocked = delVerdict !== 'AMAN' + server guard crosscheck sp_check_usage sebelum eksekusi hapus.** |
| **10 Mei 2026** | **#123** | **Snapshot `sesi-123-row-opacity-fix` ditambahkan (1 file: `DropdownGroupsTable.tsx`). Sebelum fix: `<TableRow>` punya `opacity-50` saat `!grup.is_active` — opacity diturunkan ke semua child, termasuk tombol aksi. Akibat: tombol Hapus AMAN (red-400) terlihat pink/grey-pucat untuk grup Nonaktif (test_grup_baru), user mengira disabled padahal aktif. Fix: pindahkan opacity-50 dari TableRow ke cell-cell data spesifik saja, action cell tetap full brightness.** |
| **11 Mei 2026** | **#125** | **Snapshot `sesi-125-refactor-verdict-dropdown` dimulai (1 file sejauh ini: `lib/types/usage-tracking.types.ts` sebelum tambah `SafetyStatusFullMap`). Layer 1 Refactor Verdict: buat `lib/dropdowns/verdict.ts` (pure functions + discriminated union) + `lib/dropdowns/verdict.test.ts` (8 unit test vitest) + `lib/hooks/useDeletePermission.ts` (memoized wrapper). Layer 2 dan Layer 3 akan ditambah snapshot-nya saat dikerjakan.** |
| **11 Mei 2026** | **#125** | **Snapshot `sesi-125-refactor-verdict-dropdown` Layer 2: tambah 5 file snapshot (`app/dashboard/superadmin/dropdowns/DropdownGroupsClient.tsx` + `DropdownGroupsTable.tsx` + `DropdownOptionsList.tsx` + `DropdownOptionsPanel.tsx` + `lib/hooks/useDeleteWithSafetyCheck.ts` + `components/superadmin/UsageTrackingPanel.tsx`). Refactor 5 file existing + buat 2 file baru di components/dropdowns/ (DeleteVerdictPanel + DeleteActionButton). Architecture: DropdownGroupsClient single source of truth (build SafetyStatusFullMap via bulk fetch × 2), pass map ke Table+OptionsPanel, sub-komponen GroupRow/OptionRow pakai useDeletePermission per item. useDeleteWithSafetyCheck disederhanakan jadi useDeleteConfirmDialog (HANYA dialog state, tidak lagi fetch verdict). Verdict tersebar 4 tempat → 1 tempat (verdict.ts pure functions). Layer 1 test 8/8 lulus pre-Layer 2. Menunggu npm run build + visual verify 8 skenario sebelum Layer 3.** |
| **11 Mei 2026** | **#125** | **Snapshot tambahan: `lib/hooks/useGroupDeleteCheck.ts` (DEPRECATED wrapper dari S#122). Build error pertama Layer 2 mengungkap file wrapper ini masih reference shape lama (deleteVerdicts) dari hook yang sudah disimplifikasi. Fix: file di-stub jadi `export {}` (deprecation notice + audit trail), code lama di-archive. File ini sudah TIDAK DIPAKAI setelah Layer 2 refactor karena DropdownGroupsClient pakai useDeleteConfirmDialog langsung.** |
| **11 Mei 2026** | **#126** | **Snapshot `sesi-125-refactor-verdict-dropdown/_pre-s126/` dimulai (1 file: `components/superadmin/UsageTrackingPanel.tsx` kondisi pre-S#126 = post-S#125 edit comment). S#126 Layer 3 L3.1+L3.2 INFRASTRUCTURE: buat `components/ui/sheet.tsx` (Sheet shadcn wrapper Radix Dialog umbrella, side variant) + `components/dropdowns/DropdownDetailDrawer.tsx` (Side Peek drawer: SheetHeader + DeleteVerdictPanel + UsageTrackingPanel hideVerdictHeader). Edit `UsageTrackingPanel.tsx`: tambah prop `hideVerdictHeader` default false backward compat — saat true, sembunyikan VerdictIcon+VerdictLabel+heading, container netral slate, render BreakdownSection+DependencyList+FooterNote saja. Snapshot pre-edit S#126 di sub-folder `_pre-s126/`. SISA L3.3+L3.4: wire drawer ke DropdownGroupsTable+Client+OptionsPanel (kebab menu + buka drawer dari klik "Lihat Pemetaan"). Snapshot 3 file kode (Table+Client+icons.constant.ts) BELUM disnapshot — dikerjakan di sesi berikutnya.** |
| **11 Mei 2026** | **#127** | **Snapshot `sesi-125-refactor-verdict-dropdown/_pre-s126/` dilengkapi (5 file: `DropdownGroupsTable.tsx` + `DropdownGroupsClient.tsx` + `DropdownOptionsList.tsx` + `DropdownOptionsPanel.tsx` + `lib/constants/icons.constant.ts` + `components/superadmin/UsageTrackingPanel.tsx`). S#127 L3.1+L3.2 REDO (filesystem write S#126 gagal): buat `components/ui/sheet.tsx` + `components/dropdowns/DropdownDetailDrawer.tsx` (DrawerTarget interface), edit `UsageTrackingPanel.tsx` (prop hideVerdictHeader). L3.3: tambah `more: MoreVertical` ke `ICON_ACTION`, refactor `DropdownGroupsTable.tsx` (kebab menu ⋮ gantikan tombol berjajar, hapus inline UsageTrackingPanel accordion row, tambah prop onOpenDrawer), wire `DropdownGroupsClient.tsx` (drawerTarget state + render DropdownDetailDrawer + pass onOpenDrawer ke Table). L3.4: tambah prop onOpenDrawer ke `DropdownOptionsPanel.tsx` + refactor `DropdownOptionsList.tsx` (kebab menu per opsi, hapus inline panel, Lihat Pemetaan buka drawer). Bug Sk 2 terselesaikan by design: drawer render DeleteVerdictPanel verdict SAMA dengan tombol Hapus.** |
| **12 Mei 2026** | **#132** | **Snapshot `sesi-132-m6-migration` dimulai (1 file: `lib/repositories/tenant.repository.ts` kondisi pre-M6 — 2 fungsi: findNamaBrandById + findDefaultNamaBrand, masih pakai status='aktif'). Step 3.1 Migration M6: ALTER TABLE tenants + CREATE TABLE tenant_pic_history + categories + tenant_category_assignments + 5 SP + indexes + RLS.** |
| **12 Mei 2026** | **#133** | **Snapshot `sesi-133-m6-bugfix-colgroup` ditambahkan (2 file: `app/dashboard/superadmin/tenants/TenantTable.tsx` + `app/dashboard/superadmin/categories/CategoriesClient.tsx` kondisi pre-fix). Bug identik di kedua file: JSX inline comment `{/* ... */}` setelah `<col />` di dalam `<colgroup>` → React memperlakukan sebagai expression child → text node invalid di HTML → hydration error. Fix: pindahkan comment ke SEBELUM `<col>` sebagai `{/* comment */}<col ... />` — tidak ada node setelah `<col>`, hydration bersih.** |
| **12 Mei 2026** | **#133** | **Snapshot `sesi-133-m6-sidebar-nav` ditambahkan (3 file stub: `lib/constants/icons.constant.ts` + `lib/constants/nav.constant.ts` + `components/SidebarNav.tsx` kondisi pre-S#133). Perubahan: (1) icons.constant.ts — tambah `manajemen: Building2` ke ICON_NAV. (2) nav.constant.ts — tambah grup baru `manajemen` dengan 2 item (tenants + categories) masing-masing punya `path` override. (3) SidebarNav.tsx — tambah case `isGroupActive` untuk group key `manajemen` (path includes /tenants atau /categories). DB: insert 3 message_library entries (sidebar_menu_manajemen, nav_menu_manajemen_tenant, nav_menu_master_kategori) ke kategori sidebar_ui.** |
| **12 Mei 2026** | **#138** | **Snapshot `sesi-138-m9-fix-complaint-join` ditambahkan (1 file: `lib/repositories/complaint.repository.ts` kondisi pre-fix). Bug: query `findAwaitingSuperAdmin` dan `findById` menggunakan named FK hint `tenants!complaints_tenant_scope_id_fkey` — tapi tabel complaints tidak punya FK constraint apapun (0 FK constraints terverifikasi via SQL). PostgREST gagal resolve join → RSC halaman /refunds throw error → halaman tampil pesan merah. Fix: ubah ke 3-query approach (Q1: complaints saja, Q2: tenants by tenant_scope_id, Q3: user_profiles by customer_user_id, merge manual via tenantMap + profileMap). Pola konsisten dengan pendekatan M8 untuk cross-schema join.** |
| **13 Mei 2026** | **#141** | **Snapshot `sesi-141-m6-fix` dimulai (2 file: `app/dashboard/superadmin/tenants/[id]/TabInfoUmum.tsx` + `TenantDetailClient.tsx` kondisi pre-Fase A). Fase A: fix G14–G21 Tab Info Umum — TenantDetailHeader baru + 5 cluster accordion + lifecycle visualization 5-state + per-cluster edit mode.** |
| **13 Mei 2026** | **#141** | **Snapshot `sesi-141-m6-fix` ditambah 3 file: `TabPICHistory.tsx` + `TabUserTenant.tsx` + `TabOverrideConfig.tsx` (kondisi pre-style-konsistensi). Lanjutan Fase A: sesuaikan style 3 tab placeholder dengan design token mockup (inline style, Tabler icons) supaya konsisten dengan Tab Info Umum/Kontrak Sewa/Kategori.** |
| **13 Mei 2026** | **#141** | **Snapshot `sesi-141-m6-fix` ditambah 3 file: `TabKontrakSewa.tsx` (Fase B G22-G30) + `TabKategori.tsx` (Fase C G31-G38) + `TabPICHistory.tsx` (Fase D placeholder). Style konsisten dengan TabInfoUmum (accordion, design tokens, badge style).** |
| **13 Mei 2026** | **#141** | **Snapshot `sesi-141-m6-fix` ditambah 5 file Fase F: `TenantTable.tsx` + `TenantsClient.tsx` + `DialogTambahTenant.tsx` (G01-G13 List Tenants) + `CategoriesClient.tsx` + `DialogBuatKategori.tsx` (G47-G60 List Categories). Rebuild dari shadcn Card/Badge/Button → inline design tokens konsisten dengan Detail Tenant. DialogTambahTenant: rebuild dari 2-step wizard → satu form dengan 2 section. DialogBuatKategori: tambah mode Edit + toggle Status + slug immutable info.** |
| **13 Mei 2026** | **#142** | **Snapshot `sesi-142-m6-tc-fix` — fix tambahan (2 file: `lib/repositories/tenant-category-assignment.repository.ts` + `app/.../TabKategori.tsx`). TC-08 BUG Coverage Area: `getSummaryByTenantId` tampilkan "Seluruh Indonesia" saat belum ada assignment (totalAktif===0) — menyesatkan karena terkesan sudah dikonfigurasi. Fix: bedakan BELUM_SETTING (totalAktif===0) vs Seluruh Indonesia (ada assignment, tidak ada coverage spesifik). UI card: BELUM_SETTING → card amber dengan panduan 2 langkah.** |
| **13 Mei 2026** | **#142** | **Snapshot `sesi-142-m6-tc-fix` — fix tambahan (file sama DialogTambahTenant.tsx). TC-03b BUG NPWP: (1) validasi strip non-digit + hitung 15/16 digit terlalu rigid — format bisa berubah dari kantor pajak, (2) auto-format titik/dash tidak ada. Fix: hapus hardcode digit count dari validateNPWP, validasi hanya cek tidak kosong + min 5 karakter, tambah auto-format titik-dash saat user ketik sesuai format NPWP aktual `XX.XXX.XXX.X-XXX.XXX`, help text diubah ke netral.** |
| **14 Mei 2026** | **#143** | **Snapshot `sesi-143-m6-hutang01` dimulai (1 file: `app/dashboard/superadmin/tenants/[id]/TabKategori.tsx` kondisi post-S#142). H1 HUTANG-01: implement DialogTambahKategori.tsx (dialog assign kategori G35) + wire onClick tombol "+ Tambah Kategori" di TabKategori.tsx. API POST + GET?view=tree sudah ada sejak S#132, hanya butuh UI dialog.** |
| **13 Mei 2026** | **#142** | **Snapshot `sesi-142-m6-tc-fix` dimulai (1 file: `app/dashboard/superadmin/tenants/DialogTambahTenant.tsx` kondisi post-S#141). TC-03 BUG: field "Kode Tenant" tampil sebagai input manual — seharusnya auto-generate by system dan hidden. Fix: hapus div field dari JSX, hapus `slugStatus !== 'taken'` dari canSubmit, ubah useEffect slug check agar auto-increment (append `-2`) jika slug taken — user tidak perlu tahu, sistem handle sendiri.** |
| **13 Mei 2026** | **#141** | **Snapshot `sesi-141-m6-fix` ditambah 2 file BUG TABLER ICONS: `app/layout.tsx` + `app/globals.css` (kondisi pre-fix). Bug: ikon `<i className="ti ti-X" />` di SELURUH modul M6 tampil kosong/kotak placeholder. Root cause: Next.js 16 ubah `<link rel="stylesheet">` di JSX jadi `<link rel="preload" as="style">` (preload-only, CSS rule tidak applied). Tailwind 4 PostCSS juga tidak proses external @import URL di globals.css. Fix final: React 19 native `<link>` dengan prop `precedence="default"` di `<head>` layout.tsx — prop ini membuat React pertahankan sebagai stylesheet aktif. Verifikasi via JS console: `linkRel: "stylesheet"` (bukan `"preload"`) + `document.fonts` family tabler status=`"loaded"`. Visual verify via Claude in Chrome MCP: 11 halaman + 4 dialog + 0 console error. SEMUA KODE S#141 UNCOMMITTED — commit + push S#142.** |
| **14 Mei 2026** | **#144** | **Snapshot `sesi-144-m6-master-wilayah` dimulai (3 file pre-A2): `lib/repositories/province.repository.ts` + `lib/services/province.service.ts` + `lib/constants/nav.constant.ts` kondisi pre-Master Wilayah. A2: implementasi halaman Master Wilayah (/dashboard/superadmin/wilayah) + CRUD provinsi + kota + sidebar nav entry.** |
| **14 Mei 2026** | **#145** | **Snapshot `sesi-145-fix-bug-015-tahap2` dimulai (2 file pre-fix): `app/dashboard/superadmin/layout.tsx` + `next.config.ts` kondisi pre-BUG-015 Tahap 2 fix. BUG-015 Tahap 2: hapus `export const dynamic = 'force-dynamic'` + refactor Suspense islands + tambah `experimental.optimizePackageImports`. Baseline cold start TTFB 553.71ms (terukur S#139). Target: <200ms TTFB.** |
| **14 Mei 2026** | **#146** | **Snapshot `sesi-146-fix-bug-015-tahap2-iter2` dimulai (2 file pre-fix): `lib/config-registry.ts` + `app/dashboard/superadmin/layout.tsx` kondisi pre-Fix A + Fix B. Fix A: tambah `getActiveSidebarFeatureKeys()` unstable_cache TTL 1800s di config-registry.ts + ganti raw query featureKeys di fetchSidebarData() layout.tsx. Fix B: refactor `configCache` module-level Map → `unstable_cache` TTL 300s di config-registry.ts + update `invalidateConfigCache()`. Target: cold TTFB turun dari 563ms ke <200ms.** |
| **14 Mei 2026** | **#147** | **Snapshot `sesi-147-hutang02-pic-cadangan` dimulai (1 file pre-refactor): `app/dashboard/superadmin/tenants/[id]/TabPICHistory.tsx` kondisi pre-S#147 (10.5 KB, sudah melewati batas 10 KB). HUTANG-02: implementasi wizard PIC cadangan — tombol Tambah/Edit/Hapus di kartu PIC cadangan + DialogTambahPICCadangan.tsx. TabPICHistory.tsx akan dipecah menjadi main + subcomponents.tsx. Konstanta PIC_RELASI_OPTIONS + PIC_ALASAN_OPTIONS ditambahkan ke tenant.constant.ts.** |
| **14 Mei 2026** | **#149** | **Snapshot `sesi-149-fix-dry-wa` dimulai (1 file): `app/dashboard/superadmin/tenants/DialogTambahTenant.tsx` kondisi pre-fix (14 KB, lokal autoCorrectWA DRY violation). HUTANG-DRY-01: hapus function autoCorrectWA lokal, ganti dengan import dari lib/utils-client.ts. Shared version dibuat S#147 untuk DialogTambahPICCadangan, sekarang dikonsolidasikan.** |
| **15 Mei 2026** | **#151** | **Snapshot `sesi-151-monitoring-m3-integrasi` dimulai (1 file): `app/dashboard/superadmin/providers/ProvidersClient.tsx` (22.35 KB — melebihi batas 10 KB ATURAN 9). Akan dipecah jadi 4 file: ProvidersClient.tsx (orchestrator) + ProviderListPanel.tsx + DialogTambahInstance.tsx + DialogKonfigurasiKoneksi.tsx. Sekaligus: UI grouping Koneksi Aplikasi vs Monitoring Platform + field definitions 3 provider management baru (supabase-management, github, vercel).** |
| **15 Mei 2026** | **#157** | **Snapshot `sesi-157-b1-otp-key-fix` dibuat (1 file: `lib/services/otp.service.ts` kondisi pre-fix). B1-01: `cfg['otp_expiry_minutes']` salah → harusnya `cfg['otp_expiry_seconds']` (nilai DB = 300 detik). Sekaligus rename var `otpExpiryMenit` → `otpExpiryDetik`, fix kalkulasi expiredAt (`* 1000` bukan `* 60 * 1000`), fix Redis TTL (`ex: otpExpiryDetik` bukan `ex: otpExpiryMenit * 60`), fix return `Math.round(otpExpiryDetik / 60)`. B1-02: `cfg['otp_max_attempts']` salah → harusnya `cfg['max_otp_attempts']`.** |
| **15 Mei 2026** | **#157** | **Snapshot `sesi-157-b1-account-lock-fix` dibuat (1 file: `lib/services/account-lock.service.ts` kondisi pre-fix). B1-03: `cfg['lock_duration_minutes']` salah → harusnya `cfg['lockout_duration_minutes']` (key DB = lockout_duration_minutes, nilai 30 menit). SP sp_increment_lock_count selalu terima 15 menit hardcode fallback. Semua 4 role terdampak: SA, Vendor, AT, Customer. 2 caller: prosesGagalLogin (login-action-helpers.ts) + lock-account/route.ts. Fix: 1 baris di incrementLockCount(), tidak ada perubahan caller/signature.** |
| **15 Mei 2026** | **#157** | **Snapshot `sesi-157-b1-vendor-middleware-guard` dibuat (1 file: `middleware.ts` kondisi pre-fix). B1-04 T-038: vendorStatus diekstrak dari JWT tapi tidak pernah dicek vs VENDOR_LOGIN_ALLOWED. Vendor PENDING/REVIEW bisa akses /dashboard/vendor jika JWT masih valid. Fix: tambah import VENDOR_LOGIN_ALLOWED + tambah guard setelah if(!userRole || !userId) sebelum header propagation.** |
| **15 Mei 2026** | **#158** | **Snapshot `sesi-158-t023-delete-getTenantConfig` dibuat (1 file: `lib/getTenantConfig.ts`). T-023: file legacy dari migrasi S#037 Firebase→Supabase. Nilai hardcode: primary_color '#2563eb', commission {percentage:10, minimum_amount:50000}, timers {t1:15, t2:60, t3:120}. Kroscek 20+ file — 0 caller aktif ditemukan. Pengganti sudah ada: lib/config-registry.ts (getConfigValues) + lib/policy.ts (getEffectivePolicy). File dihapus dari codebase aktif via move ke arsip.** |
| **15 Mei 2026** | **#158** | **Snapshot `sesi-158-t025-delete-policy` dibuat (1 file: `lib/policy.ts`). T-025: file legacy era Firebase migration — getEffectivePolicy() baca dari platform_policies (tabel lama). Kroscek semua caller — 0 aktif ditemukan. Pengganti: lib/config-registry.ts (getConfigValues/getConfigValue). File dihapus + tabel platform_policies di-DROP dari DB (5 baris legacy, tidak disinkronisasi sejak migrasi ke config_registry).** |
| **16 Mei 2026** | **#161** | **Snapshot `sesi-161-t017-metrics-retention-fix` dibuat (2 file: `lib/services/metrics-collector.service.ts` + `app/api/cron/collect-metrics/route.ts` kondisi pre-fix). T-017: `retentionDays: number = 30` default parameter hardcode di `collectL1Metrics` — SA tidak bisa ubah via dashboard. Fix: import `getConfigValue+parseConfigNumber` di route.ts, baca `monitoring.data_retention_days` dari config sebelum panggil `collectL1Metrics(retentionDays)`. JSDoc service diupdate. DB: `monitoring.data_retention_days = 30` sudah ada.** |
| **16 Mei 2026** | **#161** | **Snapshot `sesi-161-t016-credential-revalidate-fix` dibuat (1 file: `lib/services/credential.service.ts` kondisi pre-fix). T-016: `revalidate: 15 * 60` hardcode di 2 fungsi private (`getCredentialFromDB` + `getCredentialsByProvider`) — SA tidak bisa ubah TTL cache credential via dashboard. Fix: import `getConfigValue + parseConfigNumber` dari config-registry, baca `redis_ttl_credentials_seconds` dari `platform_general` sebelum buat `unstable_cache` di kedua fungsi. DB: `platform_general.redis_ttl_credentials_seconds = 900` sudah ada.** |
| **16 Mei 2026** | **#161** | **Snapshot `sesi-161-t014-alert-timezone-fix` dibuat (1 file: `lib/services/alert.service.ts` kondisi pre-fix). T-014: `buildAlertMessage()` hardcode `timeZone: 'Asia/Jakarta'` — SA tidak bisa ubah timezone pesan alert WA/Email. Fix: import `getPlatformTimezone` dari config-registry.ts, ubah `buildAlertMessage` menjadi async, ganti hardcode dengan `await getPlatformTimezone()`, update caller `evaluateRule` dengan `await`. DB: `platform_general.platform_timezone` sudah ada (nilai 'Asia/Jakarta'). `getPlatformTimezone()` sudah ada di config-registry.ts.** |
| **16 Mei 2026** | **#160** | **Snapshot `sesi-160-t005-monitoring-policy-key` dibuat (1 file pre-fix): `lib/services/alert.service.ts`. T-005 Opsi B: (1) DB — 10 row kategori Monitoring: UPDATE feature_key = 'monitoring' + SET policy_key per row (drop prefix 'monitoring.'). (2) Kode — refactor `getAlertTarget()` dari direct Supabase query via feature_key → getConfigValues('monitoring') dari config-registry.ts. Sebelum fix: query `.in('feature_key', ['monitoring.superadmin_alert_wa_number', 'monitoring.superadmin_alert_email'])` direct DB tanpa cache. Sesudah fix: `getConfigValues('monitoring')` dengan unstable_cache TTL 300s.** |
| **16 Mei 2026** | **#159** | **Snapshot `sesi-159-t062-tenant-status-m4` dibuat (2 file pre-fix): `app/dashboard/superadmin/tenants/TenantsClient.tsx` + `app/dashboard/superadmin/tenants/page.tsx`. T-062: STATUS_TABS hardcode → diganti dengan data dari M4 grup tenant_status. Sebelum fix: M4 punya opsi salah (trial/inactive) + tidak sinkron dengan TenantLifecycleStatus type. Fix Opsi A: (1) repair M4 DB data tenant_status + vendor_status, (2) tambah fungsi dropdownRepo_findOptionsByGroupSlug + MasterDropdownService_getOptionsByGroupSlug, (3) page.tsx fetch M4, (4) TenantsClient terima statusTabs sebagai prop.** |
| **16 Mei 2026** | **#162** | **Snapshot `sesi-162-t020-session-timeout-constant` dibuat (4 file: `lib/auth.ts` + `lib/hooks/useLoginFlow.ts` + `app/login/actions.ts` + `app/login/login-action-helpers.ts` kondisi pre-fix). T-020: magic number `480` (menit) dan `8 * 3600` (detik) tersebar di 4 file tanpa konstanta bersama — SA tidak bisa konfirmasi nilai fallback konsisten. Fix: tambah `SESSION_DEFAULT_TIMEOUT_MINUTES = 480` di `lib/auth.ts`, ekspor dan ganti semua magic number di 3 file lain.** |
| **16 Mei 2026** | **#162** | **Snapshot `sesi-162-t019-pic-notif-message` dibuat (1 file: `lib/services/tenant-pic.service.ts` kondisi pre-fix). T-019: `kirimNotifikasiGantiPIC()` hardcode pesan WA sebagai template string langsung di kode — SA tidak bisa ubah via M2 Pesan. Fix: INSERT 1 row `message_library` (key=`notif_wa_ganti_pic`, kategori=`notif_wa`, variabel=[user_name, tipe_pic, tanggal_efektif]), import `getMessage+interpolate` dari `lib/message-library`, ganti template string hardcode dengan `await getMessage('notif_wa_ganti_pic', FALLBACK)` + `interpolate()`. Pola mengikuti `notif_wa_akun_dikunci` di account-lock.service.ts.** |
| **16 Mei 2026** | **#163** | **Snapshot `sesi-163-t024-message-cache-ttl` dibuat (1 file: `lib/message-library.ts` kondisi pre-fix). T-024: `revalidate: 15 * 60` hardcode di 2 fungsi (`getMessagesByKategori` + `getMessage`) — SA tidak bisa ubah TTL cache pesan via dashboard. Chicken-egg paradox: message-library tidak bisa baca TTL-nya sendiri dari config_registry (circular dependency). Fix: ganti kedua `revalidate: 15 * 60` → `revalidate: parseInt(process.env.MESSAGE_CACHE_TTL ?? '900', 10)`. Tambah `MESSAGE_CACHE_TTL=900` di `.env.development.local` + `.env.production.local`. Nilai default `900` = sama dengan `15 * 60` lama — tidak ada perubahan behavior saat env var belum diset.** |
| **16 Mei 2026** | **#163** | **Snapshot `sesi-163-t027-t028-config-page-utils` dibuat (3 file pre-fix): `app/dashboard/superadmin/settings/security-login/page.tsx` + `app/dashboard/superadmin/settings/multi-role-policy/page.tsx` + `components/ConfigItem.tsx` kondisi pre-refactor. T-028: `mapTipe()`, `mapValue()`, `type JsonFieldConfig`, `type ConfigItemType` terduplikat identik di kedua page.tsx. Fix: ekstrak ke `lib/utils/config-page.utils.ts` (shared utils), kedua page import dari utils. T-027: `vendor_blocked_statuses` punya `tipe_data='text'` tapi `mapTipe()` tidak handle → fallthrough ke `number-unit` (salah render). Fix: tambah case `text→text-field` di utils + tambah handler `text-field` di `ConfigItem.tsx`.** |
| **16 Mei 2026** | **#164** | **Snapshot `sesi-164-t029-platform-general` dibuat (2 file pre-fix): `lib/utils/config-page.utils.ts` + `lib/constants/nav.constant.ts` kondisi pre-T-029. T-029: platform_general (5 key: platform_timezone, redis_ttl_*, sidebar_cache_ttl_seconds) tidak ada halaman dashboard. Bug tersembunyi: `mapTipe()` tidak handle `tipeData='string'` → fallthrough ke 'number-unit' (salah render timezone). Fix: (1) tambah case `string→text-field` di mapTipe(), (2) buat page platform-general/page.tsx + loading.tsx, (3) tambah nav entry, (4) INSERT message_library.** |
| **16 Mei 2026** | **#164** | **Snapshot `sesi-164-t032-monitoring-config` dibuat (2 file pre-edit): `app/dashboard/superadmin/monitoring/MonitoringClient.tsx` (15.7 KB — melebihi batas ATURAN 9) + `page.tsx` kondisi pre-T-032. T-032: tidak ada form edit 10 config monitoring. Fix: pecah MonitoringClient.tsx jadi orchestrator + buat MonitoringClient.subcomponents.tsx (SummaryCard+SectionLabel+AlertRulesPanel+MonitoringConfigPanel). page.tsx: tambah fetch config_registry monitoring + prop. Reuse /api/config/bulk (sudah ada) — tidak buat route baru.** |
| **17 Mei 2026** | **#165** | **Snapshot `sesi-165-t041-session-timeout-cookie` dibuat (1 file pre-fix): `app/login/login-action-helpers.ts` kondisi pre-T-041. T-041: `setCookiesLoginServer()` menghitung `sessionTimeoutMinutes` dari config_registry tapi TIDAK pernah set cookie `session_timeout_minutes` — hanya pakai nilainya untuk `maxAge` 4 cookie lain. Akibat: `middleware.ts` `timeoutMenit` selalu null → blok cek inactive timeout tidak pernah dieksekusi. Fix: tambah 1 baris `cookieStore.set('session_timeout_minutes', ...)` setelah 4 cookie existing. Caller: 8 call sites (5 di actions.ts + 3 di actions-legacy.ts) — tidak ada yang perlu diubah.** |
| **17 Mei 2026** | **#165** | **Snapshot `sesi-165-t048-password-min-length` dibuat (2 file pre-fix): `app/login/actions.ts` + `app/login/actions-legacy.ts` kondisi pre-T-048. T-048: `LOGIN_FORM_SCHEMA = buildLoginFormSchema()` module-level di actions.ts dibuat SEKALI saat module load dengan default `passwordMinLength = 8` — tidak baca `password_min_length` dari config_registry. SA ubah nilai di dashboard tidak ada efek. Fix actions.ts: hapus module-level constant, baca `password_min_length` dari `sessionCfg` (sudah ada di Promise.all), validate per-request. Fix actions-legacy.ts: tambah getConfigValues + pass passwordMinLength di 3 legacy functions.** |
| **17 Mei 2026** | **#166** | **Snapshot `sesi-166-t035-progressive-lockout` dibuat (2 file pre-fix): `lib/services/account-lock.service.ts` + `lib/repositories/account-lock.repository.ts` kondisi pre-T-035. T-035: `progressive_lockout_enabled = true` di config_registry tapi SP `sp_increment_lock_count` tidak punya parameter progressive — SP selalu pakai durasi lockout tetap. `max_lock_duration_hours` juga tidak dikonsumsi. Fix: (1) SP — tambah parameter `p_progressive_enabled` + `p_max_lock_duration_minutes`, hitung durasi via LEAST(base×2^lock_count, max). (2) Repository — tambah 2 parameter ke interface + rpc call. (3) Service — baca `progressive_lockout_enabled` + `max_lock_duration_hours` dari config, pass ke repository.** |
| **17 Mei 2026** | **#166** | **Snapshot `sesi-166-t036-idle-reset-counter` dibuat (2 file pre-fix, kondisi post-T-035): `lib/services/account-lock.service.ts` + `lib/repositories/account-lock.repository.ts`. T-036: `login_attempts_reset_hours = 24` di config_registry tapi SP tidak pernah cek `last_attempt_at` untuk idle reset. User gagal login 4x → idle 25 jam → coba lagi → count lanjut dari 4 (bukan reset ke 0). Fix: SP tambah parameter `p_reset_hours integer DEFAULT 0`. Di block ELSE (status=unlocked), cek idle: jika `last_attempt_at < now - p_reset_hours hours` → reset count ke 0. Repository: tambah `reset_hours?: number`. Service: baca `login_attempts_reset_hours` dari config, pass ke repository.** |
| **17 Mei 2026** | **#167** | **Snapshot `sesi-167-t039-otp-channel-routing` dibuat (4 file pre-fix): `lib/services/otp.service.ts` + `app/api/auth/send-otp/route.ts` + `lib/hooks/login/loginApiCalls.ts` + `lib/hooks/useLoginFlow.ts`. T-039: `default_otp_channel` ada di config_registry tapi `sendOTP()` selalu kirim via Fonnte WA tanpa routing logic — SA ubah channel tidak ada efek. Fix Opsi C: (1) Buat `lib/utils/smtp.server.ts` (sendSmtpOTP via nodemailer). (2) Tambah `email?` di SendOTPParams + ParamSendOTP + route schema. (3) `sendOTP()` baca `default_otp_channel` dari cfg → route ke WA (Fonnte) atau Email (SMTP). Channel SMS belum ada provider → return error informatif. (4) useLoginFlow.ts: pass `email` di kirimOTP → fetchSendOTP.** |
| **17 Mei 2026** | **#168** | **Snapshot `sesi-168-t040-require-otp-per-role` dibuat (3 file pre-fix): `app/login/login-types.ts` + `app/api/auth/send-otp/route.ts` + `lib/hooks/useLoginFlow.ts`. T-040: `require_otp` di DB = JSON per-role `{"customer":"disabled","vendor":"optional","admin_tenant":"optional","super_admin":"required"}` tapi login flow tidak pernah baca config ini — gate client-side pakai `=== 'true'` yang selalu false untuk JSON string. Fix Opsi B (dua lapis): (1) Server-side gate di `send-otp/route.ts` — baca require_otp JSON per role, query `user_profiles.app_metadata.use_otp` untuk mode optional. (2) Client-side gate di `useLoginFlow.ts` — ganti `=== 'true'` dengan `parseRequireOtpForRole()` di 2 lokasi + handle `otp_skipped` response di `kirimOTP()`. (3) Helper `parseRequireOtpForRole` + tipe `OtpMode` ditambahkan ke `app/login/login-types.ts`. Mode: required=selalu OTP, disabled=skip, optional+use_otp null/false=skip (per spec).** |
| **17 Mei 2026** | **#169** | **Snapshot `sesi-169-t049-multi-role-policy` dibuat (1 file pre-fix): `lib/services/membership.service.ts` kondisi pre-T-049. T-049: 6 key multi_role_policy (`allow_multi_role`, `max_roles_per_user`, `allow_simultaneous_roles`, `allow_multi_tenant`, `max_tenants_per_user`, `auto_assign_customer_role`) ada di config_registry tapi tidak satu pun dikonsumsi oleh `MembershipService_assignRole()`. SA ubah nilai di dashboard tidak ada efek. Fix: tambah 5 gate config check di `MembershipService_assignRole()` — baca `getConfigValues('multi_role_policy')`, fetch membership aktif user via `membershipRepo_findByUserId`, cek 5 policy (allow_multi_role, max_roles_per_user, allow_multi_tenant, max_tenants_per_user, allow_simultaneous_roles) sebelum insert. `auto_assign_customer_role` catat sebagai T-049b (registration flow, bukan MembershipService — dikerjakan terpisah).** |
| **17 Mei 2026** | **#170** | **Snapshot `sesi-170-t050-concurrent-sessions-per-role` dibuat (2 file pre-fix): `app/api/auth/check-session/route.ts` + `app/login/login-session-check.ts` kondisi pre-T-050. T-050: `max_concurrent_sessions_per_role` di multi_role_policy (JSON per-role `{"customer":-1,"vendor":-1,"admin_tenant":-1,"super_admin":1}`) tidak pernah dibaca. `check-session/route.ts` dan `login-session-check.ts` hanya baca `concurrent_rule` (global: always/different_role_only/none) yang tidak pernah memblokir login. Super Admin bisa punya >1 sesi aktif meski config menetapkan batas 1. Fix: (1) route.ts — tambah baca max_concurrent_sessions_per_role JSON, filter sesiRoleAktif per role login, jika count >= max → return blocked:true. Koreksi comment outdated tentang loginUnifiedAction. (2) login-session-check.ts — tambah `diblokir?: boolean` ke HasilCekSesiParalel, logic identik dengan route.ts.** |
| **17 Mei 2026** | **#171** | **Snapshot `sesi-171-t055-monitoring-config-keys` dibuat (2 file pre-fix): `app/api/cron/collect-metrics/route.ts` + `lib/services/metrics-collector.service.ts` kondisi pre-T-055. T-055: 7 dari 10 key monitoring.* di config_registry tidak dikonsumsi kode. Actionable: `alert_threshold_response_ms`, `alert_cooldown_minutes`, `alert_consecutive_failures` — harus dibaca dari config dan dipass ke `upsertDefaultRules()`. Kritis: `upsertDefaultRules()` sudah ada di alert-rules.repository.ts (menerima 3 param) tapi tidak pernah dipanggil dari manapun. Fix: (1) route.ts — refactor dari `getConfigValue` (1 key) ke `getConfigValues('monitoring')` (1 round-trip, 4 key), tambah baca 3 key alert config. (2) metrics-collector.service.ts — update signature `collectL1Metrics()` tambah 3 param, tambah call `upsertDefaultRules()` setelah providers diambil dari DB.** |
| **17 Mei 2026** | **#172** | **Snapshot `sesi-172-sl-d001-k001-brand-server` dibuat (2 file pre-fix): `lib/services/otp.service.ts` + `lib/services/account-lock.service.ts` kondisi pre-SL-D001+K001. SL-D001: `getNamaPlatform()` private duplikat identik di otp.service.ts (tenantId?: string) dan account-lock.service.ts (tenantId?: string|null). Logic: findNamaBrandById(tenantId) → findDefaultNamaBrand() → ''. Tidak terdaftar di cr_functions. Fix: ekstrak ke `lib/utils/brand.server.ts` → `getNamaBrandPlatform(tenantId?: string|null): Promise<string>`. SL-K001: daftarkan getNamaBrandPlatform ke cr_functions (process_type=CONFIG, domain=platform, is_shared=true).** |
| **17 Mei 2026** | **#173** | **Snapshot `sesi-173-sl-d002-k002-fonnte-server` dibuat (4 file pre-fix): `lib/services/otp.service.ts` + `lib/services/account-lock.service.ts` + `lib/services/tenant-pic.service.ts` + `lib/services/alert.service.ts` kondisi pre-SL-D002+K002. SL-D002: Pola `fetch('https://api.fonnte.com/send', ...)` duplikat inline di 4 file — baris fetch identik tapi error handling berbeda (otp: response.text() jika error; account-lock: response.json().catch(()=>({}))); tenant-pic: tidak cek response sama sekali; alert: throws Error). Fix: ekstrak ke `lib/utils/fonnte.server.ts` → `sendFonnteWA(target, message, token): Promise<{success: boolean; reason?: string}>`. Token tetap diambil caller via getCredential sebelum call fungsi. SL-K002: daftarkan sendFonnteWA ke cr_functions (process_type=NOTIFICATION, domain=notification, is_shared=true).** |
| **17 Mei 2026** | **#170** | **Snapshot `sesi-170-t052-allow-account-sharing` dibuat (2 file pre-fix): `lib/services/membership.service.ts` + `lib/repositories/user-membership.repository.ts` kondisi pre-T-052. T-052 partial: dari 7 key KYC+security di multi_role_policy, 6 key defer (require_unique_email, require_unique_phone, require_kyc_for_vendor, require_kyc_for_admin_tenant, vendor_auto_approve, periodic_access_review_days) karena bergantung fitur belum dibangun. 1 key diimplementasi: `allow_account_sharing=false` — Gate 6 di MembershipService_assignRole + fungsi baru membershipRepo_checkRoleSlotOccupied (cek apakah slot tenant+role sudah ditempati user lain yang aktif).** |
