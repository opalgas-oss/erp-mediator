# INDEX — Coding History Archive

**Folder ini menyimpan snapshot file kode SEBELUM perubahan besar/refactor.**

**Tujuan:** Memungkinkan perbandingan "sebelum vs sesudah" refactor, dan rollback manual kalau ternyata refactor menyebabkan regresi.

**Aturan yang melahirkan folder ini:** CODING_RULES_AI — **ATURAN 12: Arsip Coding Sebelum Refactor**

---

| Tanggal | Sesi | Deskripsi |
|---|---|---|
| **16 Mei 2026** | **#162** | **Snapshot `sesi-162-t019-pic-notif-message` dibuat (1 file: `lib/services/tenant-pic.service.ts` kondisi pre-fix). T-019: `kirimNotifikasiGantiPIC()` hardcode pesan WA sebagai template string langsung di kode — SA tidak bisa ubah via M2 Pesan. Fix: INSERT 1 row `message_library` (key=`notif_wa_ganti_pic`, kategori=`notif_wa`, variabel=[user_name, tipe_pic, tanggal_efektif]), import `getMessage+interpolate` dari `lib/message-library`, ganti template string hardcode dengan `await getMessage('notif_wa_ganti_pic', FALLBACK)` + `interpolate()`. Pola mengikuti `notif_wa_akun_dikunci` di account-lock.service.ts.** |
| **16 Mei 2026** | **#163** | **Snapshot `sesi-163-t024-message-cache-ttl` dibuat (1 file: `lib/message-library.ts` kondisi pre-fix). T-024: `revalidate: 15 * 60` hardcode di 2 fungsi (`getMessagesByKategori` + `getMessage`) — SA tidak bisa ubah TTL cache pesan via dashboard. Chicken-egg paradox: message-library tidak bisa baca TTL-nya sendiri dari config_registry (circular dependency). Fix: ganti kedua `revalidate: 15 * 60` → `revalidate: parseInt(process.env.MESSAGE_CACHE_TTL ?? '900', 10)`. Tambah `MESSAGE_CACHE_TTL=900` di `.env.development.local` + `.env.production.local`. Nilai default `900` = sama dengan `15 * 60` lama — tidak ada perubahan behavior saat env var belum diset.** |
| **16 Mei 2026** | **#163** | **Snapshot `sesi-163-t027-t028-config-page-utils` dibuat (3 file pre-fix): `app/dashboard/superadmin/settings/security-login/page.tsx` + `app/dashboard/superadmin/settings/multi-role-policy/page.tsx` + `components/ConfigItem.tsx` kondisi pre-refactor. T-028: `mapTipe()`, `mapValue()`, `type JsonFieldConfig`, `type ConfigItemType` terduplikat identik di kedua page.tsx. Fix: ekstrak ke `lib/utils/config-page.utils.ts` (shared utils), kedua page import dari utils. T-027: `vendor_blocked_statuses` punya `tipe_data='text'` tapi `mapTipe()` tidak handle → fallthrough ke `number-unit` (salah render). Fix: tambah case `text→text-field` di utils + tambah handler `text-field` di `ConfigItem.tsx`.** |
| **16 Mei 2026** | **#164** | **Snapshot `sesi-164-t029-platform-general` dibuat (2 file pre-fix): `lib/utils/config-page.utils.ts` + `lib/constants/nav.constant.ts` kondisi pre-T-029. T-029: platform_general (5 key: platform_timezone, redis_ttl_*, sidebar_cache_ttl_seconds) tidak ada halaman dashboard. Bug tersembunyi: `mapTipe()` tidak handle `tipeData='string'` → fallthrough ke 'number-unit' (salah render timezone). Fix: (1) tambah case `string→text-field` di mapTipe(), (2) buat page platform-general/page.tsx + loading.tsx, (3) tambah nav entry, (4) INSERT message_library.** |
| **16 Mei 2026** | **#164** | **Snapshot `sesi-164-t032-monitoring-config` dibuat (2 file pre-edit): `app/dashboard/superadmin/monitoring/MonitoringClient.tsx` (15.7 KB — melebihi batas ATURAN 9) + `page.tsx` kondisi pre-T-032. T-032: tidak ada form edit 10 config monitoring. Fix: pecah MonitoringClient.tsx jadi orchestrator + buat MonitoringClient.subcomponents.tsx (SummaryCard+SectionLabel+AlertRulesPanel+MonitoringConfigPanel). page.tsx: tambah fetch config_registry monitoring + prop. Reuse /api/config/bulk (sudah ada) — tidak buat route baru.** |
| **17 Mei 2026** | **#165** | **Snapshot `sesi-165-t041-session-timeout-cookie` dibuat (1 file pre-fix): `app/login/login-action-helpers.ts` kondisi pre-T-041. T-041: `setCookiesLoginServer()` menghitung `sessionTimeoutMinutes` dari config_registry tapi TIDAK pernah set cookie `session_timeout_minutes` — hanya pakai nilainya untuk `maxAge` 4 cookie lain. Akibat: `middleware.ts` `timeoutMenit` selalu null → blok cek inactive timeout tidak pernah dieksekusi. Fix: tambah 1 baris `cookieStore.set('session_timeout_minutes', ...)` setelah 4 cookie existing. Caller: 8 call sites (5 di actions.ts + 3 di actions-legacy.ts) — tidak ada yang perlu diubah.** |
| **17 Mei 2026** | **#165** | **Snapshot `sesi-165-t048-password-min-length` dibuat (2 file pre-fix): `app/login/actions.ts` + `app/login/actions-legacy.ts` kondisi pre-T-048. T-048: `LOGIN_FORM_SCHEMA = buildLoginFormSchema()` module-level di actions.ts dibuat SEKALI saat module load dengan default `passwordMinLength = 8` — tidak baca `password_min_length` dari config_registry. SA ubah nilai di dashboard tidak ada efek. Fix actions.ts: hapus module-level constant, baca `password_min_length` dari `sessionCfg` (sudah ada di Promise.all), validate per-request. Fix actions-legacy.ts: tambah getConfigValues + pass passwordMinLength di 3 legacy functions.** |
| **17 Mei 2026** | **#166** | **Snapshot `sesi-166-t035-progressive-lockout` dibuat (2 file pre-fix): `lib/services/account-lock.service.ts` + `lib/repositories/account-lock.repository.ts` kondisi pre-T-035. T-035: `progressive_lockout_enabled = true` di config_registry tapi SP `sp_increment_lock_count` tidak punya parameter progressive — SP selalu pakai durasi lockout tetap. `max_lock_duration_hours` juga tidak dikonsumsi. Fix: (1) SP — tambah parameter `p_progressive_enabled` + `p_max_lock_duration_minutes`, hitung durasi via LEAST(base×2^lock_count, max). (2) Repository — tambah 2 parameter ke interface + rpc call. (3) Service — baca `progressive_lockout_enabled` + `max_lock_duration_hours` dari config, pass ke repository.** |
| **17 Mei 2026** | **#166** | **Snapshot `sesi-166-t036-idle-reset-counter` dibuat (2 file pre-fix, kondisi post-T-035): `lib/services/account-lock.service.ts` + `lib/repositories/account-lock.repository.ts`. T-036: `login_attempts_reset_hours = 24` di config_registry tapi SP tidak pernah cek `last_attempt_at` untuk idle reset. User gagal login 4x → idle 25 jam → coba lagi → count lanjut dari 4 (bukan reset ke 0). Fix: SP tambah parameter `p_reset_hours integer DEFAULT 0`. Di block ELSE (status=unlocked), cek idle: jika `last_attempt_at < now - p_reset_hours hours` → reset count ke 0. Repository: tambah `reset_hours?: number`. Service: baca `login_attempts_reset_hours` dari config, pass ke repository.** |
| **17 Mei 2026** | **#167** | **Snapshot `sesi-167-t039-otp-channel-routing` dibuat (4 file pre-fix): `lib/services/otp.service.ts` + `app/api/auth/send-otp/route.ts` + `lib/hooks/login/loginApiCalls.ts` + `lib/hooks/useLoginFlow.ts`. T-039: `default_otp_channel` ada di config_registry tapi `sendOTP()` selalu kirim via Fonnte WA tanpa routing logic — SA ubah channel tidak ada efek. Fix Opsi C: (1) Buat `lib/utils/smtp.server.ts` (sendSmtpOTP via nodemailer). (2) Tambah `email?` di SendOTPParams + ParamSendOTP + route schema. (3) `sendOTP()` baca `default_otp_channel` dari cfg → route ke WA (Fonnte) atau Email (SMTP). Channel SMS belum ada provider → return error informatif. (4) useLoginFlow.ts: pass `email` di kirimOTP → fetchSendOTP.** |
| **17 Mei 2026** | **#168** | **Snapshot `sesi-168-t040-require-otp-per-role` dibuat (3 file pre-fix): `app/login/login-types.ts` + `app/api/auth/send-otp/route.ts` + `lib/hooks/useLoginFlow.ts`. T-040: `require_otp` di DB = JSON per-role tapi login flow tidak pernah baca config ini — gate client-side pakai `=== 'true'` yang selalu false untuk JSON string. Fix Opsi B (dua lapis): server-side gate di send-otp/route.ts + client-side gate di useLoginFlow.ts + helper parseRequireOtpForRole + tipe OtpMode.** |
| **17 Mei 2026** | **#169** | **Snapshot `sesi-169-t049-multi-role-policy` dibuat (1 file pre-fix): `lib/services/membership.service.ts` kondisi pre-T-049. T-049: 6 key multi_role_policy tidak dikonsumsi oleh `MembershipService_assignRole()`. Fix: tambah 5 gate config check di MembershipService_assignRole().** |
| **17 Mei 2026** | **#170** | **Snapshot `sesi-170-t050-concurrent-sessions-per-role` dibuat (2 file pre-fix): `app/api/auth/check-session/route.ts` + `app/login/login-session-check.ts`. T-050: `max_concurrent_sessions_per_role` JSON per-role tidak pernah dibaca. Fix: enforce hard block per role di route.ts + login-session-check.ts.** |
| **17 Mei 2026** | **#170** | **Snapshot `sesi-170-t052-allow-account-sharing` dibuat (2 file pre-fix): `lib/services/membership.service.ts` + `lib/repositories/user-membership.repository.ts` kondisi pre-T-052. T-052 partial: allow_account_sharing Gate 6 + membershipRepo_checkRoleSlotOccupied.** |
| **17 Mei 2026** | **#171** | **Snapshot `sesi-171-t055-monitoring-config-keys` dibuat (2 file pre-fix): `app/api/cron/collect-metrics/route.ts` + `lib/services/metrics-collector.service.ts`. T-055: 7 dari 10 key monitoring.* tidak dikonsumsi. Fix: route.ts refactor ke getConfigValues('monitoring') + call upsertDefaultRules().** |
| **17 Mei 2026** | **#172** | **Snapshot `sesi-172-sl-d001-k001-brand-server` dibuat (2 file pre-fix): `lib/services/otp.service.ts` + `lib/services/account-lock.service.ts`. SL-D001: getNamaPlatform() private duplikat identik → ekstrak ke lib/utils/brand.server.ts.** |
| **17 Mei 2026** | **#173** | **Snapshot `sesi-173-sl-d002-k002-fonnte-server` dibuat (4 file pre-fix): otp + account-lock + tenant-pic + alert service. SL-D002: pola fetch Fonnte duplikat → ekstrak ke lib/utils/fonnte.server.ts → sendFonnteWA().** |
| **17 Mei 2026** | **#170** | **Snapshot `sesi-170-t052-allow-account-sharing` dibuat (2 file pre-fix): `lib/services/membership.service.ts` + `lib/repositories/user-membership.repository.ts` kondisi pre-T-052 partial.** |
| **18 Mei 2026** | **#174** | **Snapshot `sesi-174-sl-d003-k003-validation-server` dibuat (2 file pre-fix): `lib/services/tenant.service.ts` + `lib/services/tenant-pic.service.ts`. SL-D003: validateNomorWa() private duplikat → ekstrak ke lib/utils/validation.server.ts.** |
| **18 Mei 2026** | **#174** | **Snapshot `sesi-174-sl-d007-routes-constant` dibuat (3 file pre-fix): `lib/auth.ts` + `lib/constants/index.ts` + snapshot ROLE_REDIRECT dari middleware.ts. SL-D007: ROLE_DASHBOARD+ROLE_REDIRECT → ekstrak ke lib/constants/routes.constant.ts → ROLE_TO_DASHBOARD.** |
| **18 Mei 2026** | **#175** | **Snapshot `sesi-175-sl-d010-k010-validate-dropdown-slug` dibuat (2 file pre-fix): `lib/services/master-dropdown-group.service.ts` + `lib/services/master-dropdown-option.service.ts`. SL-D010: validateSlug() private duplikat → ekstrak ke lib/utils/validation.server.ts → validateDropdownSlug().** |
| **18 Mei 2026** | **#175** | **Snapshot `sesi-175-sl-d011-validate-sort-order` dibuat (2 file pre-fix, kondisi post-SL-D010): `lib/services/master-dropdown-group.service.ts` + `lib/services/master-dropdown-option.service.ts`. SL-D011: validateSortOrder() private duplikat → ekstrak ke lib/utils/validation.server.ts → validateSortOrder().** |
| **18 Mei 2026** | **#176** | **Snapshot `sesi-176-lr3-cr-constants` dibuat (3 file pre-fix): `lib/auth.ts` + `app/auth/logout-action.ts` + `lib/constants/index.ts`. LR-3: TTL_PRESETS+ROLE_TO_DASHBOARD+SESSION_COOKIE_NAMES belum di cr_constants. Proaktif: SESSION_COOKIES duplikat COOKIES_LOGOUT → ekstrak ke lib/constants/session.constant.ts.** |
| **18 Mei 2026** | **#177** | **Snapshot `sesi-177-pv03-category-repo` dibuat (2 file pre-fix): `lib/repositories/tenant-category-assignment.repository.ts` + `lib/services/category.service.ts`. PV-03: CategoryService_hapus() query DB langsung di service layer — pelanggaran Repository Pattern.** |
| **18 Mei 2026** | **#178** | **Snapshot `sesi-178-t060b-tenant-tier` dibuat (5 file pre-fix): `lib/types/tenant.types.ts` + `lib/services/tenant.service.ts` + tenants/page.tsx + TenantsClient.tsx + DialogTambahTenant.tsx. T-060b: M4 tenant_tipe misaligned ke TenantTier.** |
| **18 Mei 2026** | **#178** | **Snapshot `sesi-178-t059b-gps-mode` dibuat (1 file pre-fix): `lib/hooks/useLoginFlow.ts`. T-059b: gps_mode config_registry konsep salah (select English) → boolean toggle.** |
| **18 Mei 2026** | **#179** | **Snapshot `sesi-179-pv04-tenant-repo-pattern` dibuat (2 file pre-fix): `lib/services/tenant.service.ts` + `lib/repositories/tenant.repository.ts` kondisi pre-PV-04. PV-04: `TenantService_create()` memanggil `createServerSupabaseClient()` + `db.rpc('sp_create_tenant_with_pic', {...})` langsung di service layer — pelanggaran Repository Pattern. Fix: (1) Tambah `tenantRepo_createWithPIC(payload, createdBy)` ke tenant.repository.ts — wrap rpc call ke repo. (2) tenant.service.ts: hapus direct db.rpc + hapus import createServerSupabaseClient, ganti dengan panggil tenantRepo_createWithPIC. Caller: hanya 1 (app/api/superadmin/tenants/route.ts POST) — tidak perlu diubah.** |
| **18 Mei 2026** | **#179** | **Snapshot `sesi-179-pv05-pv06-tenant-pic-repo-pattern` dibuat (2 file pre-fix): `lib/services/tenant-pic.service.ts` + `lib/repositories/tenant-pic.repository.ts` kondisi pre-PV-05+PV-06. PV-05: `TenantPICService_gantiPIC()` bypass `jalankanGantiPICViaSP()` yang sudah ada di repo — direct db.rpc di service. Bug proaktif: `jalankanGantiPICViaSP` hardcode `p_changed_by: null` padahal changedBy tersedia. PV-06: `TenantPICService_tambahCadangan()` juga direct db.rpc di service — tidak ada repo function untuk operasi ini. Fix: (1) Update `jalankanGantiPICViaSP` — tambah param `changedBy: string | null`, hapus null hardcode. (2) Buat `tenantPicRepo_tambahCadangan(input, addedBy)` di repo. (3) Service: import kedua fungsi repo, hapus 2 blok direct db.rpc, hapus import createServerSupabaseClient.** |
| **18 Mei 2026** | **#179** | **Snapshot `sesi-179-pv07-pv08-tca-repo-pattern` dibuat (2 file pre-fix): `lib/services/tenant-category-assignment.service.ts` + `lib/repositories/tenant-category-assignment.repository.ts` kondisi pre-PV-07+PV-08. PV-07: `TCAService_assign()` direct `createServerSupabaseClient()+.from('assignment_coverage_areas').insert()` di service layer setelah `assignViaSP()` — insert coverage areas langsung tanpa repo. PV-08: `TCAService_updateOverrideKomisi()` direct `createServerSupabaseClient()+.from('tenant_category_assignments').update()` di service. Fix: (1) Buat `tcaRepo_insertCoverageAreas(assignmentId, entries)` di repo. (2) Buat `tcaRepo_updateOverrideKomisi(assignmentId, payload, updatedBy)` di repo. (3) Service: import keduanya, hapus 2 blok direct DB, hapus import createServerSupabaseClient.** |
| **18 Mei 2026** | **#177** | **Snapshot `sesi-177-pv09-pv10-config-page-items` dibuat (4 file pre-fix): `lib/config-registry.ts` + `security-login/page.tsx` + `multi-role-policy/page.tsx` + `platform-general/page.tsx` kondisi pre-PV-09+PV-10+proaktif. PV-09+PV-10: 3 RSC page settings query `config_registry` langsung via createServerSupabaseClient di page layer — pelanggaran Repository Pattern + Service Layer. Temuan proaktif: platform-general/page.tsx (dibuat T-029 S#164) pola identik tapi belum di audit list. Temuan bug: multi-role-policy punya `.eq('is_active', true)` — inkonsistensi dengan pola S#110 (SA wajib lihat semua item). Fix: tambah `getConfigPageItems(featureKey)` + interface `ConfigRegistryFullItem` ke lib/config-registry.ts. 3 page diedit: hapus createServerSupabaseClient + direct query, ganti panggil getConfigPageItems(). multi-role-policy: hapus juga filter is_active=true yang salah.** |
| **18 Mei 2026** | **#180** | **Snapshot `sesi-180-sl-d009-classify-http-error` dibuat (13 file pre-fix): semua route SA yang pakai keyword-based error classification. SL-D009+K009: keyword-matching 400 vs 500 tersebar di 13 route SA dengan keyword berbeda antar route. Fix: buat `lib/utils/http.server.ts` → `classifyHttpError()`. Update 13 catch block + fix 2 bug. cr_functions INSERT. File diarsip: 13 route SA.** |
| **18 Mei 2026** | **#180** | **Snapshot `sesi-180-sl-d004-fetch-with-timeout` dibuat (2 file pre-fix): `lib/services/provider-tester.ts` + `lib/services/metrics-collector.service.ts` kondisi pre-SL-D004+K004. SL-D004: `makeController()` private helper di provider-tester.ts (dipakai 7 fungsi test via `.finally(clear)`) + inline AbortController di `pingProvider()` dari metrics-collector.service.ts — pola fetch-timeout duplikat dengan abstraksi berbeda. Bug proaktif: `pingProvider()` `clearTimeout(t)` tidak dieksekusi di catch block → timer leak saat network error (AbortController abort tidak bermasalah, tapi network error sebelum timeout tidak clear timer). Fix: buat `lib/utils/fetch.server.ts` → `fetchWithTimeout(url, init, timeoutMs)` dengan `finally { clearTimeout(timer) }` — no timer leak. Update provider-tester.ts: hapus `makeController()` + `TIMEOUT_MS` const, ganti 7 call sites ke fetchWithTimeout. Update metrics-collector.service.ts: hapus inline AbortController + setTimeout + clearTimeout di pingProvider(), ganti fetchWithTimeout — sekaligus fix bug timer leak. cr_functions INSERT fetchWithTimeout (CONFIG/infrastructure, is_shared=true).** |
| **18 Mei 2026** | **#181** | **Snapshot `sesi-181-sl-d005-build-id-map` dibuat (2 file pre-fix): `lib/repositories/complaint.repository.ts` + `lib/repositories/user-membership.repository.ts` kondisi pre-SL-D005+K006. SL-D005: `Object.fromEntries(array.map(x=>[x.id,x]))` duplikat di 3 occurrences — 2 di `complaintRepo_findAwaitingSuperAdmin` (tenantMap + profileMap) + 1 di `membershipRepo_findAll` (profileMap). Fix: buat `lib/utils/db.utils.ts` → `buildIdMap<T extends {id:string}>(items: T[]): Record<string, T>`. Update 3 call sites. cr_functions INSERT buildIdMap (CONFIG/infrastructure, is_shared=true).** |
