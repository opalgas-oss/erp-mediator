# INDEX — Coding History Archive

**Folder ini menyimpan snapshot file kode SEBELUM perubahan besar/refactor.**

**Tujuan:** Memungkinkan perbandingan "sebelum vs sesudah" refactor, dan rollback manual kalau ternyata refactor menyebabkan regresi.

**Aturan yang melahirkan folder ini:** CODING_RULES_AI — **ATURAN 12: Arsip Coding Sebelum Refactor**

---

| Tanggal | Sesi | Deskripsi |
|---|---|---|
| **12 Mei 2026** | **#133** | **Snapshot `sesi-133-m6-bugfix-colgroup` dibuat (2 file): `app/dashboard/superadmin/tenants/TenantTable.tsx` + `app/dashboard/superadmin/categories/CategoriesClient.tsx`. FIX colgroup hydration error: JSX `{/* komentar */}` setelah `<col />` di dalam `<colgroup>` menghasilkan text node invalid di HTML — React hydration mismatch. Fix: pindahkan komentar ke SEBELUM `<col />`. Ditemukan saat TC-M6-01 dan TC-M6-02 FASE 5 verifikasi M6. ⚠️ CATATAN S#221 AUDIT: Folder ada di disk tapi ISI KOSONG — file tidak pernah disalin ke dalamnya. Arsip tidak dapat digunakan untuk rollback.** |
| **12 Mei 2026** | **#133** | **Snapshot `sesi-133-m6-sidebar-nav` dibuat (3 file): `lib/constants/icons.constant.ts` + `lib/constants/nav.constant.ts` + `components/SidebarNav.tsx`. TAMBAH SidebarNav grup Manajemen: icons.constant.ts tambah `manajemen: Building2` ke ICON_NAV. nav.constant.ts tambah grup `manajemen` dengan 2 item (Manajemen Tenant `/tenants`, Master Kategori `/categories`). SidebarNav.tsx tambah case isGroupActive untuk group key `manajemen`. DB: INSERT 3 message_library entries (nav_group_manajemen, nav_tenant_mgmt, nav_category_master). ⚠️ CATATAN S#221 AUDIT: Folder ada di disk tapi ISI KOSONG — file tidak pernah disalin ke dalamnya. Arsip tidak dapat digunakan untuk rollback.** |
| **12 Mei 2026** | **#134** | **⚠️ PELANGGARAN ATURAN 12: `lib/constants/icons.constant.ts` di-EDIT (2x: tambah pengguna:Shield + fix GPS icon terhapus) tanpa arsip. Tidak ada folder `sesi-134-*` di coding-history. File baru sesi ini (21 file: types + repo + service + API routes + UI) tidak wajib diarsip — arsip hanya untuk FILE YANG DIEDIT. Dampak: tidak ada snapshot pre-S#134 untuk icons.constant.ts. CATATAN AUDIT S#221.** |
| **12 Mei 2026** | **#135** | **⚠️ PELANGGARAN ATURAN 12: 9 file di-EDIT tanpa arsip. File yang diedit: `lib/constants/nav.constant.ts` + `components/SidebarNav.tsx` (Step 3.7) + `app/api/superadmin/permissions/[id]/route.ts` + `app/api/superadmin/roles/[id]/route.ts` + `app/api/superadmin/roles/[id]/permissions/route.ts` + `app/api/superadmin/roles/[id]/permissions/[pid]/route.ts` + `app/dashboard/superadmin/roles/[id]/page.tsx` (build fix) + `lib/repositories/permissions.repository.ts` + `lib/repositories/roles.repository.ts` (RawRow cast). Claude menyatakan “tidak ada arsip yang diperlukan karena penambahan kecil” — ini RASIONALISASI SALAH. ATURAN 12 berlaku untuk semua edit file kode, bukan hanya “refactoring besar”. CATATAN AUDIT S#221.** |
| **12 Mei 2026** | **#136** | **PELANGGARAN ATURAN 12: 2 file di-EDIT tanpa arsip: lib/constants/nav.constant.ts + components/SidebarNav.tsx (Step 3.7 M8 - tambah memberships ke grup pengguna + isGroupActive case). 13 file BARU tidak wajib diarsip. Tidak ada folder sesi-136-* di coding-history. CATATAN AUDIT S#221 (audit S#136).** |
| **12 Mei 2026** | **#137** | **PELANGGARAN ATURAN 12: 3 file di-EDIT tanpa arsip: lib/constants/icons.constant.ts (tambah ICON_FINANCE.refund, grup baru) + lib/constants/nav.constant.ts (tambah refunds ke grup pengguna) + components/SidebarNav.tsx (isGroupActive cover /refunds). Tidak ada folder sesi-137-* di coding-history. CATATAN AUDIT S#221 (audit S#137).** |
| **24 Mei 2026** | **#209** | **Snapshot `sesi-209-otp-only-fase3` diupdate (tambah 2 file): `app/dashboard/superadmin/settings/security-login/page.tsx` + `components/PerRoleJsonEditor.tsx`. Step 7: security-login/page.tsx tambah `otp_only` ke options require_otp + require_otp_superadmin, urutan jadi ['disabled','required','otp_only'] per TDD-Q1. Step 8: PerRoleJsonEditor.tsx tambah konstanta OTP_MODE_LABEL + update SelectItem render dari `{opt}` ke `{OTP_MODE_LABEL[opt] ?? opt}`. Pending Step 9: npm run build (Philips eksekusi di terminal). Arsip terdahulu di folder ini: app/login/login-types.ts (Step 1) + lib/hooks/useLoginFlow.ts (Step 5). ⚠️ ATURAN 12 violation: app/login/actions.ts di-EDIT di S#209 (Step 3: tambah initOtpOnlyAction + finishOtpOnlyAction) tapi TIDAK ada di folder arsip ini. Tidak ada snapshot pre-S#209 untuk actions.ts.** |
| **22 Mei 2026** | **#201** | **Snapshot `sesi-201-revert-magic-link` dibuat (2 file POST-S#200): `app/login/components/LoginFormStage.tsx` + `app/auth/verify/page.tsx`. REVERT keputusan Philips: Magic Link (OPSI B) dihapus seluruhnya — flow 7-langkah tidak user-friendly untuk marketplace jasa Indonesia. 3 file direvert: (1) LoginFormStage.tsx → restore pre-S#200 (hapus import sendMagicLinkAction, hapus showMagicLink state, hapus tombol "Masuk Tanpa Password"), (2) verify/page.tsx → restore pre-S#200 (hapus isMagicLink conditional, kembali ke Reset Password only), (3) actions.ts → hapus sendMagicLinkAction block + MagicLinkResult interface dari tail file. ⚠️ CATATAN AUDIT S#221: `actions.ts` (file ke-3 yang diedit) TIDAK diarsip di folder ini — arsip hanya mencakup 2 file. ATURAN 12 violation untuk actions.ts. Snapshot post-S#200 actions.ts tidak tersedia.** |
| **22 Mei 2026** | **#200** | **Snapshot `sesi-200-magic-link` dibuat (2 file pre-fix): `app/auth/verify/page.tsx` + `app/login/components/LoginFormStage.tsx`. OPSI B Magic Link Auth FASE 2: tambah `sendMagicLinkAction` di actions.ts (flow paralel, tidak replace password login). Reuse /auth/confirm + /auth/verify anti-scanner pattern yang sudah ada. LoginFormStage tambah toggle 'Masuk Tanpa Password' + form email-only + success state. `app/auth/verify/page.tsx` tambah conditional text untuk type=magiclink vs type=recovery. Pre-condition: Resend custom SMTP + email template Supabase dikustomisasi. Estimasi gain via magic link path: TTFB ~300-500ms (bypass bcrypt 835ms).** |
| **21 Mei 2026** | **#197** | **Snapshot `sesi-197-rek-c-preconnect` dibuat (1 file pre-fix): app/layout.tsx (1423 bytes). REK-C: tambah `<link rel="preconnect">` + `<link rel="dns-prefetch">` ke NEXT_PUBLIC_SUPABASE_URL di root layout `<head>`. Tujuan: browser mulai TLS handshake lebih awal sebelum loginUnifiedAction butuh koneksi Supabase auth endpoint. crossOrigin="anonymous" untuk cross-origin request. SKIP Upstash: UPSTASH_REDIS_REST_URL bukan NEXT_PUBLIC_ — private, tidak aman di HTML client. Estimasi gain: −50 sampai −150ms TTFB pertama hit Supabase (range, validasi 1× test produksi). Source: web.dev preconnect guide + Vercel KB + artifact compass_artifact S#196.** |
| **21 Mei 2026** | **#196** | **Snapshot `sesi-196-rek-a-parallel-getgeo` dibuat (1 file pre-fix): app/login/actions.ts. REK-A: pindahkan `getGeoForAudit()` dari sequential SEBELUM `buatSupabaseSSR()` ke dalam `Promise.all` paralel dengan `cekLockAwal` + `signInWithPassword` + `getConfigValues`. Justifikasi: research S#196 — `getGeoForAudit` adalah wrapper `headers()` read-only (no I/O, no exception, no side-effect per lib/geo-server.ts). Estimasi gain: −30 sampai −100ms TTFB warm (range, validasi di 3× test produksi).** |
| **16 Mei 2026** | **#160** | **Snapshot `sesi-160-t005-monitoring-policy-key` dibuat (1 file): `lib/services/alert.service.ts` (8.29 KB, pre-T005-OpsiB). T-005 RENCANA-FIX-AUDIT: feature_key 10 row monitoring.* diubah ke 'monitoring' + policy_key per row (10 UPDATE DB) + alert.service.ts getAlertTarget() direfactor dari query langsung config_registry → getConfigValues('monitoring'). Commit d1ba78f. PV-01 SELESAI sebagai side effect. Vercel Preview Ready 1m 8s.** |
| **16 Mei 2026** | **#161** | **Snapshot `sesi-161-t014-alert-timezone-fix` dibuat (1 file): `lib/services/alert.service.ts` (8.43 KB, pre-T014). T-014 RENCANA-FIX-AUDIT: buildAlertMessage() timezone hardcode 'Asia/Jakarta' → getPlatformTimezone(). Import getPlatformTimezone ditambah, buildAlertMessage jadi async, evaluateRule diupdate await. Commit c58e5fb. Vercel Preview Ready 1m 12s.** |
| **16 Mei 2026** | **#161** | **Snapshot `sesi-161-t016-credential-revalidate-fix` dibuat (1 file): `lib/services/credential.service.ts` (8.98 KB, pre-T016). T-016 RENCANA-FIX-AUDIT: getCredentialFromDB + getCredentialsByProvider revalidate 15*60 hardcode → getConfigValue('platform_general', 'redis_ttl_credentials_seconds', '900'). Commit 0fe4753. Vercel Preview Ready 48s.** |
| **16 Mei 2026** | **#161** | **Snapshot `sesi-161-t017-metrics-retention-fix` dibuat (2 file): `lib/services/metrics-collector.service.ts` (12.33 KB) + `app/api/cron/collect-metrics/route.ts`. T-017 RENCANA-FIX-AUDIT: collectL1Metrics retentionDays=30 hardcode → getConfigValue('monitoring', 'data_retention_days') dibaca di cron route + dipass ke service. JSDoc diupdate. Commit 641d24b. Vercel Preview Ready 46s.** |
| **16 Mei 2026** | **#162** | **Snapshot `sesi-162-t019-pic-notif-message` dibuat (1 file): `lib/services/tenant-pic.service.ts`. T-019 RENCANA-FIX-AUDIT: kirimNotifikasiGantiPIC pesan WA hardcode → getMessage('notif_wa_ganti_pic')+interpolate({user_name, tipe_pic, tanggal_efektif}). DB: INSERT notif_wa_ganti_pic ke message_library. Commit 279f86f. Vercel Preview Ready 51s.** |
| **16 Mei 2026** | **#162** | **Snapshot `sesi-162-t020-session-timeout-constant` dibuat (4 file): `lib/auth.ts` + `lib/hooks/useLoginFlow.ts` + `app/login/actions.ts` + `app/login/login-action-helpers.ts`. T-020 RENCANA-FIX-AUDIT: magic number 480/8*3600 session timeout tersebar di 4 file → SESSION_DEFAULT_TIMEOUT_MINUTES = 480 const di lib/auth.ts, diimport 3 file lain. Commit 2da8357. Vercel Preview Ready 1m 6s.** |
| **16 Mei 2026** | **#163** | **Snapshot `sesi-163-t024-message-cache-ttl` dibuat (1 file): `lib/message-library.ts`. T-024 RENCANA-FIX-AUDIT: revalidate 15*60 hardcode di getMessagesByKategori + getMessage → parseInt(process.env.MESSAGE_CACHE_TTL ?? '900', 10). MESSAGE_CACHE_TTL=900 ditambah ke .env.development.local + .env.production.local + Vercel All Environments. Production redeploy CMCoXh1Nr. Commit 7db7e89. Vercel Preview Ready 50s.** |
| **16 Mei 2026** | **#163** | **Snapshot `sesi-163-t027-t028-config-page-utils` dibuat (3 file): `app/dashboard/superadmin/settings/security-login/page.tsx` + `app/dashboard/superadmin/settings/multi-role-policy/page.tsx` + `components/ConfigItem.tsx`. T-028: mapTipe()+mapValue() duplikat di 2 halaman → ekstrak ke lib/utils/config-page.utils.ts (file baru). T-027: tambah 'text-field' ke type union ConfigItem + mapTipe() case 'text'. Commit d935ee9. Vercel Preview Ready 51s.** |
| **16 Mei 2026** | **#164** | **Snapshot `sesi-164-t029-platform-general` dibuat (2 file): `lib/utils/config-page.utils.ts` + `lib/constants/nav.constant.ts`. T-029 RENCANA-FIX-AUDIT: buat halaman /settings/platform-general (5 key platform_general di DB) + mapTipe case 'string'→'text-field' + nav entry platform_general + INSERT nav_menu_platform_general ke message_library. Commit 2ffb4b6. Vercel Preview Ready 47s.** |
| **17 Mei 2026** | **#164** | **Snapshot `sesi-164-t032-monitoring-config` dibuat (2 file): `app/dashboard/superadmin/monitoring/MonitoringClient.tsx` + `app/dashboard/superadmin/monitoring/page.tsx`. T-032 RENCANA-FIX-AUDIT: MonitoringClient.tsx dipecah (15.7 KB→7.7 KB) + MonitoringClient.subcomponents.tsx baru (10.1 KB) + monitoring/page.tsx fetch config_registry monitoring + prop initialMonitoringConfig. Reuse /api/config/bulk untuk save (tidak buat route baru). Commit ea5a18f. Vercel Preview Ready 47s.** |
| **17 Mei 2026** | **#165** | **Snapshot `sesi-165-t041-session-timeout-cookie` dibuat (1 file): `app/login/login-action-helpers.ts`. T-041 RENCANA-FIX-AUDIT: setCookiesLoginServer() tidak pernah set cookie `session_timeout_minutes` → tambah cookie ke-5 setelah 4 cookie existing. middleware.ts inactive timeout sekarang aktif. Commit 7dc2ff6. Vercel Preview Ready 46s.** |
| **17 Mei 2026** | **#165** | **Snapshot `sesi-165-t048-password-min-length` dibuat (2 file): `app/login/actions.ts` + `app/login/actions-legacy.ts`. T-048 RENCANA-FIX-AUDIT: Hapus LOGIN_FORM_SCHEMA module-level constant, baca password_min_length dari sessionCfg per-request setelah Promise.all. actions-legacy.ts: tambah import getConfigValues + baca pwMin di 3 legacy functions. Commit 85c686c. Vercel Preview Ready 1m 4s.** |
| **17 Mei 2026** | **#166** | **Snapshot `sesi-166-t035-progressive-lockout` dibuat (2 file): `lib/services/account-lock.service.ts` + `lib/repositories/account-lock.repository.ts` (pre-T035). T-035 RENCANA-FIX-AUDIT: SP sp_increment_lock_count + repo + service diupdate untuk baca `progressive_lockout_enabled` + `max_lock_duration_hours`. Formula progressive: LEAST(base×2^lock_count, max). Drop overload 7-param lama. Commit 940d809. Vercel Preview Ready 51s.** |
| **17 Mei 2026** | **#166** | **Snapshot `sesi-166-t036-idle-reset-counter` dibuat (2 file): `lib/services/account-lock.service.ts` + `lib/repositories/account-lock.repository.ts` (post-T035, pre-T036). T-036 RENCANA-FIX-AUDIT: SP sp_increment_lock_count + repo + service diupdate untuk baca `login_attempts_reset_hours`. Idle reset: jika last_attempt_at < now - N hours DAN status=unlocked → reset count ke 0. Drop overload 9-param lama, SP kini 10 parameter (final). Commit 5d872fb. Vercel Preview Ready 47s. — Entry ditambahkan retroaktif saat audit S#221.** |
| **17 Mei 2026** | **#167** | **Snapshot `sesi-167-t039-otp-channel-routing` dibuat (4 file pre-fix): `lib/services/otp.service.ts` + `app/api/auth/send-otp/route.ts` + `lib/hooks/login/loginApiCalls.ts` + `lib/hooks/useLoginFlow.ts`. T-039 RENCANA-FIX-AUDIT: buat `lib/utils/smtp.server.ts` (nodemailer@^8.0.7, server-only) + routing OTP channel di otp.service.ts (whatsapp→Fonnte / email→SMTP / lainnya→error). Tambah email? di 3 file. DB: INSERT notif_email_otp_login. cr_functions: sendSmtpOTP (NOTIFICATION/otp). npm audit: nodemailer HIGH resolved (^6.x → ^8.0.7). Commit 590cb9f. Vercel Preview Ready 51s. — Entry ditambahkan retroaktif saat audit S#221.** |
| **17 Mei 2026** | **#168** | **Snapshot `sesi-168-t040-require-otp-per-role` dibuat (3 file pre-fix): `app/login/login-types.ts` + `app/api/auth/send-otp/route.ts` + `lib/hooks/useLoginFlow.ts`. T-040 RENCANA-FIX-AUDIT: parseRequireOtpForRole() helper (OtpMode type + JSON per-role parsing) + server-side gate di send-otp/route.ts (disabled→otp_skipped, optional→query use_otp kolom, required→sendOTP) + client-side fix 2 lokasi di useLoginFlow.ts. DB: ALTER TABLE user_profiles ADD COLUMN use_otp BOOLEAN NOT NULL DEFAULT FALSE (menggantikan app_metadata JSONB ambigu). Schema_Auth.md diupdate (ATURAN 27). cr_functions: parseRequireOtpForRole (AUTH/login). Commit 3afb815. Vercel Preview Ready 1m 6s. — Entry ditambahkan retroaktif saat audit S#221.** |
| **17 Mei 2026** | **#169** | **Snapshot `sesi-169-t049-multi-role-policy` dibuat (1 file pre-fix): `lib/services/membership.service.ts`. T-049 RENCANA-FIX-AUDIT: tambah 5 gate multi_role_policy di MembershipService_assignRole(): Gate 1 allow_multi_role, Gate 2 max_roles_per_user, Gate 3 allow_simultaneous_roles, Gate 4 allow_multi_tenant, Gate 5 max_tenants_per_user. Baca getConfigValues('multi_role_policy') + filter membership aktif. T-049b (auto_assign_customer_role) deferred ke sprint registration flow — dicatat di Pekerjaan_Langkah_Auth_Login.md DEFER-T049B. Commit d2c2d19. Vercel Preview Ready 1m 4s. — Entry ditambahkan retroaktif saat audit S#221.** |
| **17 Mei 2026** | **#170** | **Snapshot `sesi-170-t050-concurrent-sessions-per-role` dibuat (2 file pre-fix): `app/api/auth/check-session/route.ts` + `app/login/login-session-check.ts`. T-050: enforce max_concurrent_sessions_per_role JSON per-role + koreksi JSDoc outdated. Tambah gate: sesiRoleAktif.length >= maxForRole → blocked:true. Commit af3de37. Vercel Preview Ready 1m 11s.** |
| **17 Mei 2026** | **#170** | **Snapshot `sesi-170-t052-allow-account-sharing` dibuat (2 file pre-fix): `lib/repositories/user-membership.repository.ts` + `lib/services/membership.service.ts`. T-052 PARTIAL: enforce allow_account_sharing gate (Gate 6 di MembershipService_assignRole). 6 defer keys terdokumentasi: DEFER-T052-AUTH-1/2 → Pekerjaan_Langkah_Auth_Login.md, DEFER-T052-AT-1/2/3 → Pekerjaan_Langkah_AdminTenant.md, PL-I06 → Pekerjaan_Langkah_Infrastruktur.md. NOTE: 5 dari 6 defer entries (AUTH-1/2 + AT-1/2/3) tidak tersimpan di S#170 — ditambahkan retroaktif saat audit S#221. Commit 3ed8d15. Vercel Preview Ready 50s. — Entry ditambahkan retroaktif saat audit S#221.** |
| **17 Mei 2026** | **#171** | **Snapshot `sesi-171-t055-monitoring-config-keys` dibuat (2 file pre-fix): `app/api/cron/collect-metrics/route.ts` + `lib/services/metrics-collector.service.ts`. T-055 RENCANA-FIX-AUDIT: refactor route ke getConfigValues('monitoring') tunggal + baca 3 key baru (alert_threshold_response_ms, alert_cooldown_minutes, alert_consecutive_failures). Update signature collectL1Metrics() + tambah call upsertDefaultRules() yang selama ini tidak pernah dipanggil. Verifikasi SL-D008+PV-01+PV-02 dilakukan S#171 via baca file aktual — semua LULUS [X] dikonfirmasi. INSTRUKSI_ATURAN_v1.md diupdate: prinsip baru "field tampil tapi diabaikan → dashboard menipu SA" + POLA MASALAH + YANG TIDAK BOLEH. Commit 6b86512. Vercel Preview Ready 1m 7s. — Entry ditambahkan retroaktif saat audit S#221.** |
| **17 Mei 2026** | **#172** | **Snapshot `sesi-172-sl-d001-k001-brand-server` dibuat (2 file pre-fix): `lib/services/otp.service.ts` + `lib/services/account-lock.service.ts`. SL-D001+K001 RENCANA-FIX-AUDIT: getNamaPlatform() duplikat di kedua service → ekstrak ke `lib/utils/brand.server.ts` sebagai fungsi shared getNamaBrandPlatform(tenantId?: string | null). Import tenant.repository (findNamaBrandById+findDefaultNamaBrand) dipindah ke utils. cr_functions: getNamaBrandPlatform (CONFIG/platform, is_shared=true). Commit 2d8d269. Vercel Preview Ready 50s. — Entry ditambahkan retroaktif saat audit S#221.** |
| **17 Mei 2026** | **#173** | **Snapshot `sesi-173-sl-d002-k002-fonnte-server` dibuat (4 file pre-fix): otp + account-lock + tenant-pic + alert service. SL-D002: pola fetch Fonnte duplikat → ekstrak ke lib/utils/fonnte.server.ts → sendFonnteWA().** |
| **18 Mei 2026** | **#174** | **Snapshot `sesi-174-sl-d003-k003-validation-server` + `sesi-174-sl-d007-routes-constant` dibuat.** |
| **18 Mei 2026** | **#175** | **Snapshot `sesi-175-sl-d010-k010-validate-dropdown-slug` + `sesi-175-sl-d011-validate-sort-order` dibuat.** |
| **18 Mei 2026** | **#176** | **Snapshot `sesi-176-lr3-cr-constants` dibuat. LR-3: TTL_PRESETS+ROLE_TO_DASHBOARD+SESSION_COOKIE_NAMES.** |
| **18 Mei 2026** | **#177** | **Snapshot `sesi-177-pv03-category-repo` + `sesi-177-pv09-pv10-config-page-items` dibuat.** |
| **18 Mei 2026** | **#178** | **Snapshot `sesi-178-t060b-tenant-tier` + `sesi-178-t059b-gps-mode` dibuat.** |
| **18 Mei 2026** | **#179** | **Snapshot `sesi-179-pv04~pv08` dibuat (3 folder).** |
| **18 Mei 2026** | **#180** | **Snapshot `sesi-180-sl-d009-classify-http-error` + `sesi-180-sl-d004-fetch-with-timeout` dibuat.** |
| **18 Mei 2026** | **#181** | **Snapshot `sesi-181-sl-d006` + `sesi-181-sl-d005` dibuat.** |
| **19 Mei 2026** | **#182** | **Snapshot `sesi-182-otp-fix-stale-state-remove-optional` dibuat (3 file): useLoginFlow.ts + login-types.ts + send-otp/route.ts. FIX-1: selesaiLogin stale state. FIX-2: hapus mode 'optional' OTP. ⚠️ ATURAN 12 violation: security-login/page.tsx DIEDIT (hapus 'optional' dari options require_otp) tapi TIDAK ada di arsip sesi-182 — 4 file diedit, hanya 3 diarsip. ⚠️ ATURAN 23 violation: `git add` tidak menyertakan `_arsip/` folder — arsip orphan (tidak ter-commit di 8405fb8). Commit 8405fb8.** |
| **19 Mei 2026** | **#183** | **Snapshot `sesi-183-sa-otp-enforce` dibuat (5 file): `app/login/actions.ts` + `lib/hooks/useLoginFlow.ts` + `app/api/auth/load-user-profile/route.ts` + `app/api/auth/verify-otp/route.ts` + `middleware/middleware.ts`. FIX-3a: SA bypass 2 path + Vendor refactor lanjutSetelahRole. FIX-3b: load-user-profile SA nomor_wa hardcode '' → SELECT nomor_wa + ambil array[0]. FIX-3c: verify-otp tenant_id min(1) menolak SA tenantId='' → hapus min(1) konsisten dengan send-otp. FIX-3d: actions.ts SA OTP=required tidak set cookie sebelum OTP. FIX-3e: middleware.ts otp_pending cookie guard. 4 commit: daec7cf+88eaa2d+995e400+96430a8.** |
| **19 Mei 2026** | **#184** | **Snapshot `sesi-184-biometric-optional` dibuat (1 file): `app/dashboard/superadmin/settings/security-login/page.tsx`. HUTANG-BIOMETRIC-OPTIONAL: hapus mode 'optional' dari biometric_mode options → ['required', 'disabled']. Konsisten dengan require_otp (FIX-2 S#182).** |
| **19 Mei 2026** | **#184** | **Snapshot `sesi-184-sa-config-separation` dibuat (4 file): `app/login/login-types.ts` + `app/login/actions.ts` + `app/api/auth/send-otp/route.ts` + `lib/hooks/useLoginFlow.ts` (note). HUTANG-SA-CONFIG-SEPARATION: pisah config key require_otp+biometric_mode dari SA. Tambah getRequireOtpConfigKey(). 5 titik kode diupdate + 4 operasi DB + page.tsx entry baru.** |
| **19 Mei 2026** | **#184** | **Snapshot `sesi-184-fix-per-role-allowed-roles` dibuat (2 file): `components/PerRoleJsonEditor.tsx` + `lib/utils/config-page.utils.ts`. BUG FIX: PerRoleJsonEditor hardcode 4 role tanpa allowedRoles filter → blank dropdowns + risiko data corruption. Fix: tambah allowedRoles prop ke JsonFieldConfig+ConfigItemData+PerRoleJsonEditor. ⚠️ ATURAN 12 violation: `components/ConfigItem.tsx` + `app/dashboard/superadmin/settings/security-login/page.tsx` juga diedit di commit 7e0495a tapi TIDAK ada di arsip (folder security-login/ dan components/ConfigItem.tsx tidak tersimpan). Commit 7e0495a.** |
| **19 Mei 2026** | **#184** | **COMMIT 9802af1: fix(ui): sembunyikan Tenant Admin toggle untuk config SA-only. File diedit: `components/ConfigItem.tsx` (hideTenantOverrideToggle prop) + `app/dashboard/superadmin/settings/security-login/page.tsx` (set hideTenantOverrideToggle: true untuk SA-only keys). ⚠️ ATURAN 12 violation: TIDAK ADA arsip dibuat sebelum edit. ⚠️ ATURAN 23 violation: `_arsip/` TIDAK disertakan dalam git add (git add hanya 2 file kode). Tidak ada folder sesi-184-hide-tenant-toggle atau entry INDEX ini sebelum audit S#221.** |
| **19 Mei 2026** | **#185** | **Snapshot `sesi-185-fix-sa-otp-disabled` dibuat (1 file): `lib/hooks/useLoginFlow.ts`. BUG FIX: regresi S#184 — SA OTP=disabled masih masuk OTP flow di client. Root cause: configLogin tidak punya key 'require_otp_superadmin' (RLS filter + default state tidak diupdate saat S#184). Fix: handleLogin percaya result.redirectTo dari server (bukan re-check configLogin) sebagai indikator OTP=disabled.** |
| **19 Mei 2026** | **#185** | **Snapshot `sesi-185-otp-enforcement-vendor-at-customer` dibuat (1 file): `app/login/actions.ts`. FIX OTP enforcement Vendor/AdminTenant/Customer: setCookiesLoginServer dipanggil sebelum OTP diverifikasi. Bug tambahan Vendor sub-path 2: setCookies dalam Promise.all sebelum cek vendor status.** |
| **19 Mei 2026** | **#185** | **⚠️ ATURAN 12 violation: `lib/constants/session.constant.ts` diedit di commit b1c5468 (tambah 'otp_pending' ke SESSION_COOKIE_NAMES) TANPA arsip. Tidak ada folder sesi-185-otp-pending-logout di _arsip/. Dicatat retroaktif saat audit S#221.** |
| **19 Mei 2026** | **#187** | **Snapshot `sesi-187-fix-cold-start-login` dibuat (1 file): `app/api/keep-warm/route.ts`. FIX BUG-021 Layer 1: keep-warm fan-out GET-only tidak warm POST Server Action bundle `loginUnifiedAction` → cold start 31.77s. Strategi fix: (1) tambah file BARU `app/api/auth/warmup-login-action/route.ts` yang import semua modul login (tanpa side-effect) agar Server Action bundle ter-inisialisasi via cron, (2) tambah URL endpoint baru ke array `targets` di keep-warm/route.ts. Klasifikasi dampak: RENDAH (0 internal caller di kode, hanya 2 cron eksternal cron-job.org + GitHub Actions yang tidak terdampak karena signature GET + auth + response shape tetap).** |
| **20 Mei 2026** | **#190** | **Snapshot `sesi-190-fix-warmup-import` dibuat (1 file): `app/api/auth/warmup-login-action/route.ts`. FIX BUG-021 Layer 2 Step 0 BARU: warmup endpoint S#188 tidak mengimport `loginUnifiedAction` sendiri — Server Action chunk tidak ter-warm. Temuan T2 ANALISIS_CODE_REVIEW_SESI189.md. Fix: tambah 5 import baru (loginUnifiedAction, ROLES+ACCOUNT_LOCK_STATUS, SESSION_DEFAULT_TIMEOUT_MINUTES, parseRequireOtpForRole+getRequireOtpConfigKey, formatLockUntilWIB+hitungTujuanRedirectServer) + void reference-nya di GET handler.** |
| **20 Mei 2026** | **#190** | **Snapshot `sesi-190-timing-log` dibuat (2 file): `app/login/actions.ts` + `app/login/login-action-helpers.ts`. Step 1-2 BUG-021 Layer 2: tambah performance.now() per stage di loginUnifiedAction — 5 label [S190]: buatSupabaseSSR, Promise.all-block, decodeAppClaims, setCookiesLoginServer (SA path), total-action. Tambah after-tasks-total di jalankanAfterTasksLogin untuk verifikasi H4 (apakah after() benar non-blocking). AKAN DIHAPUS setelah data timing terkumpul.** |
| **20 Mei 2026** | **#191** | **Snapshot `sesi-191-sanity-check` dibuat (1 file): `app/login/actions.ts`. Step 4 (5.6) BUG-021 Layer 2 Fase 1: tambah sanity check minimal pre-Promise.all (`email.includes('@') && password.length >= 6`) untuk short-circuit input invalid sebelum 3 DB calls. Default fallback 6 (bukan parseConfigNumber — sessionCfg belum tersedia). Validasi penuh tetap berjalan setelah Promise.all dengan nilai aktual dari config_registry.** |
| **20 Mei 2026** | **#191** | **Snapshot `sesi-191-redirect-conditional` dibuat (2 file): `app/login/actions.ts` (post-Step4) + `lib/hooks/useLoginFlow.ts`. Step 6 (5.8) BUG-021 Layer 2 Fase 1: ganti 5 `return { ok: true, redirectTo: ... }` di jalur OTP=disabled (SA, Vendor-sub1, Vendor-sub2, AdminTenant, Customer) dengan `redirect()` dari `next/navigation` — eliminasi round-trip client `router.push`. `useLoginFlow.ts`: tambah komentar safety fallback di blok `result.redirectTo` (kode tidak dihapus, dilindungi untuk edge case).** |
| **20 Mei 2026** | **#192** | **Snapshot `sesi-192-revert-step-6` dibuat (1 file): `app/login/actions.ts` (state POST-Step4 + Step6 = S#191 REGRESI, 15734 bytes). Revert Step 6 redirect() anti-pattern — dikonfirmasi 5 sumber otoritatif S#191 (Next.js docs + Discussion #82080 best practice resmi + Discussion #65900 middleware double exec + Issue #57257 double render + Dario Djuric blog). Regresi S#191: +160 to +220ms warm vs baseline S#190 1.02s. Restore ke state POST-Step4 PRE-Step6 dari arsip `sesi-191-redirect-conditional`. Step 4 sanity check DIPERTAHANKAN.** |
| **21 Mei 2026** | **#194** | **Snapshot `sesi-194-fix-gps-login-blocking` dibuat (5 file): `app/login/actions.ts` (14375 bytes) + `app/login/actions-legacy.ts` (9580 bytes, ditambah saat FASE 3 build error) + `lib/hooks/useLoginFlow.ts` (25555 bytes) + `lib/hooks/login/loginSessionHelpers.ts` (5429 bytes) + `lib/hooks/useGPS.ts` (2775 bytes). FIX HUTANG-GPS-BLOCKING: eliminasi 781ms GPS+Nominatim blocking dari critical path login. Hybrid Layer 1: replace client-side getGPSLocation+Nominatim dengan server-side x-vercel-ip-city header di loginUnifiedAction. useGPS.ts dikonfirmasi TIDAK DIPAKAI (legacy dari S#049, tidak ada caller di seluruh codebase) → akan dihapus. actions-legacy.ts (dead code, tidak ter-register di cr_functions) butuh fix konsisten karena import LoginActionParams.gpsKota — ditemukan saat build (gap analisis FASE 2). Target: user actual wait login 1.567s → ~786ms (-50%). **Tambahan restore copy:** `_arsip/useGPS.ts` (4232 bytes) di root `_arsip/` — diletakkan Claude atas permintaan eksplisit Philips sebagai "antisipasi kalau Hybrid Approach gagal/bottleneck". Berisi original code + instruksi cara revert lengkap (6 langkah). DUPLIKAT dari salinan ATURAN 12 resmi, namun lokasi root memudahkan akses cepat tanpa navigasi nested folder.** |
| **23 Mei 2026** | **#205** | **Snapshot `sesi-205-fix-bug-017-018-019` dibuat (4 file): `lib/repositories/otp.repository.ts` + `lib/services/otp.service.ts` + `lib/hooks/useLoginFlow.ts` + `app/login/login-types.ts`. FIX BUG-018 (CRITICAL): tambah server-side attempt counter Redis `otp_attempts:{uid}:{tenantId}` di verifyAndConsume() — max dari config max_otp_attempts. FIX BUG-019 (HIGH): tambah resend counter Redis `otp_resend:{uid}:{tenantId}` di sendOTP() — max dari config max_otp_resend (baru diinsert ke config_registry). FIX BUG-017 (HIGH): hapus setOtpPercobaan(0) dari kirimOTP() + tambah handle result MAX_ATTEMPTS di handleVerifikasiOTP(). ⚠️ CATATAN AUDIT S#221: INDEX entry awal S#205 hanya menyebut 3 file (login-types.ts tidak disebut padahal diarsip). Dikoreksi ke 4 file retroaktif.** |
| **23 Mei 2026** | **#206** | **Snapshot `sesi-206-revert-spec-bug017-018` dibuat (1 file + CATATAN): `lib/services/otp.service.ts` (versi MENGANDUNG spec violation — del attemptsRedisKey di post-send + komentar palsu). CATATAN_ARSIP.md mendokumentasikan kondisi useLoginFlow.ts. REVERT S#205 spec violation: HAPUS `redis.del(attemptsRedisKey)` dari sendOTP() post-send + HAPUS `setOtpPercobaan(0)` dari kirimOTP(). Keputusan Philips S#206 Opsi A: attempt counter PERSIST saat resend OTP.** |
| **23 Mei 2026** | **#206** | **Snapshot `sesi-206-fix-otpstage-max-attempts` dibuat (1 file): `app/login/components/OTPStage.tsx`. Bug: tombol Kirim Ulang tidak disembunyikan saat batasPercobaan=true — countdown habis → user bisa klik Kirim Ulang meski sudah 3x salah. Fix: wrap section kirim ulang dengan `{!batasPercobaan && ...}`. Sekaligus fix: tambah state otpExpiryMenit di useLoginFlow.ts + pass `{menit}` ke pesan otp_error_batas_habis.** |
| **23 Mei 2026** | **#206** | **Snapshot `sesi-206-fix-expired-counter-gap` (CATATAN_ARSIP.md). Bug logic gap: `verifyAndConsume()` di `lib/services/otp.service.ts` saat OTP expired di Redis (storedCode=null) fall through ke postgreSQL SP → return EXPIRED tanpa increment attempts counter. Akibat: 3x submit OTP tidak mencapai MAX_ATTEMPTS jika OTP expired di tengah. Fix: setiap submit tetap increment counter (kasus EXPIRED dan WRONG), cek apakah newAttempts >= maxAttempts → return MAX_ATTEMPTS langsung.** |
| **23 Mei 2026** | **#206** | **Snapshot `sesi-206-fix-sendotp-attempts-check` (CATATAN_ARSIP.md). Bug yang ditemukan saat test refresh+login: `sendOTP()` di otp.service.ts tidak cek `otp_attempts` counter sebelum kirim OTP baru. User MAX_ATTEMPTS bisa request OTP baru via refresh+login (resend belum max). Fix: cek attempts SEBELUM cek resend, kalau ≥ max return error dengan pesan otp_error_batas_habis (interpolated menit). Tambah errorCode field di response. Client setTahap('KREDENSIAL') saat MAX_ATTEMPTS.** |
| **24 Mei 2026** | **#207** | **Snapshot `sesi-207-fix-resend-ttl` (CATATAN_ARSIP.md). Bug TTL resend counter: S#205 menulis `expire(resendRedisKey, otpExpiryDetik * (maxResend + 1))` = 12 menit — invensi tanpa dasar spec. Spec asli Bug_Sesi_085.md BUG-019: TTL = session timeout = otp_expiry_seconds. Fix: `expire(resendRedisKey, otpExpiryDetik)` = 3 menit. Satu baris di lib/services/otp.service.ts. Ditemukan saat TC-REVERT-03 S#207.** |
| **24 Mei 2026** | **#208** | **Snapshot `sesi-208-audit-hardcode-otpstage` dibuat (5 file): `app/login/components/OTPStage.tsx` + `LoginFormStage.tsx` + `RoleSelectorStage.tsx` + `BiometricStage.tsx` (pre-audit). AUDIT HARDCODE S#208: OTPStage 7 key baru otp_ui, LoginFormStage 10 key baru login_ui (fix bug inline akun dikunci → KotakError+m()), RoleSelectorStage 4 key baru login_ui, BiometricStage 4 key baru login_ui (+ 1 key shared login_button_memproses DRY). Semua komponen tambah prop m, page.tsx diupdate 3 call site. SesiParalelStage+shared.tsx CLEAN — tidak diubah.** |
| **24 Mei 2026** | **#213** | **Snapshot `sesi-213-status-popup` dibuat (CATATAN_ARSIP.md + 3 file diubah + 1 file baru). HUTANG-LOGIN-STATUS-POPUP: ganti redirect error vendor_not_approved dengan pop-up informatif per kondisi register_status + lifecycle_status. LoginActionResult tambah statusDetail. actions.ts: 3 jalur diupdate (Vendor sub1+sub2 + AdminTenant). useLoginFlow.ts: state statusPopup + handle statusDetail. StatusRegistrasiModal.tsx: komponen baru. page.tsx: render modal.** |
| **24 Mei 2026** | **#212** | **Snapshot `sesi-212-status-redesign` dibuat (11 file): ... + `app/dashboard/vendor/layout.tsx`. BUG FIX tambahan: vendor/layout.tsx fallback query .select('status') -> .select('register_status'). Ditemukan saat test 7.6 — Vendor APPROVED bisa masuk OTP tapi setelah verifikasi redirect ke login?error=vendor_not_approved karena kolom 'status' tidak ada.** |
| **25 Mei 2026** | **#215** | **Snapshot `sesi-215-otp-trigger` dibuat (2 file): `app/login/components/LoginFormStage.tsx` + `app/login/page.tsx`. FIX GAP S#214: LoginFormStage tidak punya trigger ke LoginFormOtpOnly. Tambah prop `onMasukOtpOnly?: () => void` + UI link "Masuk dengan WhatsApp OTP" (kondisional — hanya tampil jika prop di-pass). page.tsx: tambah prop onMasukOtpOnly ke LoginFormStage. INSERT 2 key message_library: login_link_wa_otp + login_separator_atau.** |
| **25 Mei 2026** | **#215** | **Snapshot `sesi-215-kirim-ulang-aktivasi` dibuat (4 file baru/marker): StatusRegistrasiModal.tsx + CATATAN_ARSIP_actions.md + marker useLoginFlow. FITUR: Kirim Ulang Email Aktivasi — tombol di pop-up belum aktivasi. File baru: aktivasi-actions.ts + app/api/activate/route.ts. Perubahan: actions.ts (+user_email ke statusDetail), useLoginFlow.ts (+user_email ke state), StatusRegistrasiModal.tsx (+button+call action). INSERT 3 key message_library.** |
| **25 Mei 2026** | **#216** | **Snapshot `sesi-216-fix-otp-lifecycle` dibuat (1 file): `lib/utils/otp-only.server.ts`. FIX BUG OTP WA lifecycle: `lookupUserByNomorWa()` hanya filter `.eq('register_status', 'approved')` tanpa cek `lifecycle_status`. User approved+lifecycle=pending bisa masuk via WA OTP. Fix: tambah `.eq('lifecycle_status', 'active')`.** |
| **25 Mei 2026** | **#217** | **Snapshot `sesi-217-fix-get-all-by-provider` dibuat (2 file): `lib/repositories/credential.repository.ts` + `lib/services/credential.service.ts`. FIX HUTANG ARSITEKTUR STEP 2: `getAllByProvider()` di repository tidak select `encrypted_dek`. `getCredentialsByProvider()` di service pakai `dekripsi()` (simple) untuk SEMUA fields padahal semua tersimpan via `enkripsiCredential()` (envelope). Fix: tambah `encrypted_dek?: string | null` ke interface CredWithDef + select + return. Update service: jika DEK ada -> `dekripsiCredential(dek, value)`, jika tidak -> `dekripsi(value)` fallback backward-compat. Temuan proaktif: 7/16 credentials punya null DEK (fonnte 2, supabase 2, upstash 2, xendit 1) semua punya env fallback, tidak blocking.** |
| **25 Mei 2026** | **#218** | **Snapshot `sesi-218-tambah-provider` dibuat (2 file): `lib/types/provider.types.ts` + `app/dashboard/superadmin/providers/ProvidersClient.tsx`. FITUR TAMBAH PROVIDER: TambahProviderPayload + TambahFieldDefPayload ditambah ke provider.types.ts. insertProvider + insertFieldDef ditambah ke credential.repository.ts. tambahProvider ditambah ke credential.service.ts. File baru: app/api/superadmin/providers/route.ts (POST, validasi nama+kategori+tag+field_defs, handle UNIQUE violation 409). DialogTambahProvider.tsx (dialog UI sesuai mockup v2: identitas + tabel field dinamis, expand per baris, auto-generate key dari label). ProvidersClient.tsx: tombol '+ Tambah Provider' di header + state showTambah + onTambahSuccess + router.refresh().** |
| **26 Mei 2026** | **#219** | **Snapshot `sesi-219-fix-bug-026` dibuat (2 file): `lib/types/tenant.types.ts` + `components/superadmin/tenants/TenantDetailHeader.tsx`. FIX BUG-026: TenantLifecycleStatus tidak include 'in_registration' + STATUS_STYLE di TenantDetailHeader tidak punya key 'in_registration'. Akibat: halaman detail tenant Jaya Motor crash (TypeError: Cannot read properties of undefined). Fix: tambah 'in_registration' ke TenantLifecycleStatus type + tambah entry STATUS_STYLE. ⚠️ KOREKSI AUDIT S#221: folder `sesi-219-fix-bug-026` aktual berisi 3 file arsip (tenant.types.ts + TenantDetailHeader.tsx + `app/dashboard/superadmin/tenants/TenantTable.tsx`), bukan 2. Selain itu 2 file DIEDIT TANPA ARSIP (ATURAN 12 violation): `lib/constants/tenant.constant.ts` (ditulis via write_file) + `lib/services/tenant.service.ts` (validTransitions, diedit via edit_file) — keduanya bagian fix BUG-026 (5 file total) tapi tidak disalin ke arsip. Commit 42b8066.** |
| **26 Mei 2026** | **#220** | **Snapshot `sesi-220-fix-bug-023-024-025` dibuat (3 file): `app/login/actions.ts` (marker) + `app/login/aktivasi-actions.ts` (full PRE-fix copy) + `lib/hooks/useLoginFlow.ts` (marker). FIX BUG-023: security login bypass approved+in_registration — 4 baris: outer gate Vendor+AT `=== 'pending'` → `!== 'active'`, inner condition tambah `|| 'in_registration'`. FIX BUG-024: OTP WA selalu salah karena JWT block — tambah verifyOtpOnlyAction Server Action di actions.ts + tambah import verifyAndConsume + update handleVerifikasiOTP di useLoginFlow.ts (kondisional isOtpOnlyFlow). FIX BUG-025: email aktivasi gagal karena status='pending' vs CHECK constraint — 1 baris ganti 'pending' → 'sent'. Base commit: 42b8066. ⚠️ KOREKSI AUDIT S#221 (ATURAN 12 violation, LL#30 S#220): arsip `actions.ts` (1.09 KB) + `useLoginFlow.ts` (803 B) hanya MARKER/komentar — BUKAN full copy konten (file aktual ~20 KB / ~24 KB, verified ukuran disk saat audit). Hanya `aktivasi-actions.ts` (5.30 KB) full PRE-fix copy. Marker bukan arsip valid — audit trail rollback tidak berguna. Arsip rusak permanen (file aktual sudah POST-fix, tidak bisa di-recover retroaktif).** |
| **25 Mei 2026** | **#216** | **Snapshot `sesi-216-fix-credential-decode` dibuat (2 file marker): `lib/repositories/credential.repository.ts` + `lib/services/credential.service.ts`. FIX ROOT CAUSE SESUNGGUHNYA: `simpanCredential` pakai `enkripsiCredential` (envelope: DEK per field) tapi `getAllByProvider` + `testKoneksi` pakai `dekripsi` (simple, Master Key langsung) — MISMATCH format. `dekripsi(envelope_ciphertext)` throw GCM auth error → catch → skip → password=undefined → 'Credential tidak lengkap'. Fix: tambah `getCredentialsByInstanceId` di repo (include encrypted_dek), tambah `dekripsiCredential` import di service, update `testKoneksi` pakai `dekripsiCredential(encrypted_dek, encrypted_value)` untuk semua fields.** | dibuat (1 file): `lib/services/credential.service.ts`. FIX BUG cache stale: `simpanCredential()` tidak memanggil `revalidateTag` setelah simpan credentials ke DB. Akibat: `testKoneksi` langsung setelah simpan membaca dari `unstable_cache` lama (‘{}’) karena instance pertama (is_default=false) sudah populate cache dengan hasil kosong. Fix: tambah `revalidateTag('credentials')` di akhir `simpanCredential` + import revalidateTag dari next/cache.** |
| **30 Mei 2026** | **#221** | **Snapshot `sesi-221-step2-otp-mode-both` dibuat (5 file — full copy): `app/login/login-types.ts` + `components/PerRoleJsonEditor.tsx` + `app/dashboard/superadmin/settings/security-login/page.tsx` + `app/login/page.tsx` + `lib/hooks/useLoginFlow.ts`. STEP 2 S#221 mode Both OTP: tambah `both` ke OtpMode type + parseRequireOtpForRole, update label `disabled`->'Tanpa OTP' + tambah `both`:'Keduanya', tambah `both` ke options dropdown per role, handle both di lanjutSetelahRole + showWaOtpLink, kontrol visibilitas onMasukOtpOnly di login page. Arsip pre-edit ATURAN 12.** |
| **1 Juni 2026** | **#230** | **Snapshot `sesi-230-rename-tenant-admintenant-history` dibuat (1 file): `lib/repositories/tenant-pic.repository.ts`. RUNTIME-BREAKING FIX: tabel `tenant_pic_history` di-RENAME ke `tenant_admintenant_history` via apply_migration, tapi 5x `.from('tenant_pic_history')` di repository belum diupdate. Fix: 5 referensi tabel + 1 header komentar diupdate via edit_file. Sisa referensi lama (kolom pic_name/tipe_pic di tenant.repository.ts + tenant.types.ts dll) NON-BREAKING (kolom masih ada di DB) — difix sebagai bagian STEP 2 migration K-18/K-19.** |
| **1 Juni 2026** | **#238** | **Snapshot `sesi-238-hutang-at-auth` dibuat (6 file — full copy pre-edit STEP 2 HUTANG-AT-AUTH): `lib/types/tenant.types.ts` + `lib/types/tenant-pic.types.ts` + `lib/repositories/tenant.repository.ts` + `lib/repositories/tenant-pic.repository.ts` + `app/dashboard/superadmin/tenants/TenantTable.tsx` + `app/dashboard/superadmin/tenants/[id]/TenantDetailClient.tsx`. Sebelum migration DDL K-18/K-19 (rename 4 kolom kontak tenants + refactor tenant_admintenant_history drop tipe_pic + index baru) + sebelum SP baru (sp_tambah_admintenant, sp_ganti_penanggung_jawab) + sebelum types/repo/service/route/UI baru AdminTenant.** |
| **2 Juni 2026** | **#240** | **Snapshot `sesi-240-hutang-at-auth-step-b` dibuat (2 file — full copy pre-edit STEP B): `components/superadmin/tenants/TenantDetailHeader.tsx` + `app/dashboard/superadmin/tenants/[id]/TenantDetailClient.tsx`. Sebelum STEP B: ganti TenantTabId 'pic'→'admintenant' + TABS label 'PIC & Riwayat'→'AdminTenant' + ganti import/render TabPICHistory → TabAdminTenantHistory + tambah prop tenantNama.** |
| **2 Juni 2026** | **#242** | **Snapshot `sesi-242-fix-bug-029-030-031-032` dibuat (5 file arsip — marker+partial). Fix BUG-031: admin-tenant-create.service.ts — tambah INSERT ke activation_email_logs (status sent/failed) sesudah kirimEmailAktivasi. Fix BUG-030: user-membership.repository.ts — tambah fungsi membershipRepo_upsertActive (UPDATE jika row inactive ada, INSERT jika belum); admin-tenant.service.ts + admin-tenant-create.service.ts — ganti membershipRepo_insert+checkExisting dengan upsertActive. Fix BUG-029: admin-tenant.types.ts — tambah lifecycle_status ke AdminTenantKartu; admin-tenant.repository.ts — getAktifByTenantId JOIN user_profiles ambil lifecycle_status; admin-tenant.service.ts — buildKartu pakai lifecycle_status===active bukan user_id!==null. BUG-032 pending (Dialog B4 v2 implementasi — setelah build confirm).** |
| **2 Juni 2026** | **#240** | **Snapshot `sesi-240-hutang-at-auth-step-c` dibuat (9 file STEP C — arsip+hapus file lama PIC): `DialogHapusPICCadangan.tsx` + `DialogTambahPICCadangan.tsx` + `TabPICHistory.subcomponents.tsx` + `TabPICHistory.tsx` + `tenant-pic.service.ts` (pre-strip) + `tenant-pic.repository.ts` (pre-strip, pre-FIX-T2) + `tenant-pic.types.ts` (pre-strip) + `change-pic-cadangan-route.ts` + `change-pic-route.ts`. STEP C: hapus konsep PIC cadangan + fix T2 (row.tipe_pic referensi kolom sudah DROP via migration K-19). Sisakan stub 3 file service/repo/types sementara.** |
