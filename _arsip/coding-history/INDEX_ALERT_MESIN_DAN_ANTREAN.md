# INDEX_ALERT_MESIN_DAN_ANTREAN.md
# Arsip Coding History — Alert & Monitoring — MESIN: service, repository, antrean WA/Email, pengumpul metrik
# Lokasi: _arsip/coding-history/INDEX_ALERT_MESIN_DAN_ANTREAN.md
# Induk navigasi: _arsip/coding-history/INDEX.md — JANGAN tulis entri di induk (ATURAN 36)
# Lahir: Sesi #427 — 31 Juli 2026, pemecahan INDEX.md 42.653 B (104,1% ambang 40.960 B) atas keputusan Philips K-427-1.
#   Sumbu pemecahan = KLASTER FITUR/MODUL, bukan urutan waktu. Alasan: pertanyaan kerja yang nyata
#   adalah "berkas yang mau saya sentuh ini pernah diarsip di mana?", dan waktu tidak menjawab itu.
#   Baris entri dipindah MEKANIS byte-exact dari salinan ber-checksum — nol karakter diketik ulang.
#
# DIKERJAKAN SETELAH: _arsip/coding-history/INDEX.md (induk navigasi)
# NEXT SETELAH INI:   folder snapshot yang disebut di baris entri
# BLOCKER:            Tidak ada

## GUNA FILE INI
Semua arsip yang menyentuh JALUR KERJA alert: lifecycle insiden (M3/M4/M8), antrean & pengiriman (M6, drain WA/Email), pengumpul metrik, dan repository alert_log / alert_rules. Buka file ini sebelum menyentuh `alert*.service.ts`, `alert*.repository.ts`, `alert-queue*`, `metrics-collector*`.

| Tanggal | Sesi | Keterangan |
|---|---|---|
| **7 Juli 2026** | **S#333** | **Snapshot `sesi-333-m3-deduplication` dibuat. M3 Deduplication: arsip alert-log.repository.ts + alert.service.ts pra-M3. incrementAlertOccurrence() + dedup logic di evaluateRule(). Arsip: 2 file.** |
| **7 Juli 2026** | **S#334** | **Snapshot `sesi-334-m6-alert-queue` dibuat. M6 Alert Queue: arsip alert.service.ts pra-integrasi enqueue (kirim langsung → enqueue Redis WA+Email). Arsip: 1 file (alert.service.ts).** |
| **8 Juli 2026** | **S#336** | **Snapshot `sesi-336-m4-maintenance-window` dibuat. M4 Maintenance Window: arsip alert.service.ts pra-integrasi maintenance window check. Arsip: 1 file (alert.service.ts).** |
| **8 Juli 2026** | **S#337** | **Snapshot `sesi-337-fix-sse-l1filter` dibuat. FIX SSE timeout: stream/route.ts tambah MAX_AGE 240s + event close sebelum Vercel timeout. MonitoringClient.tsx handle event close → reconnect 1s tanpa crash. monitoring.types.ts tambah type close ke MetricSSEEvent. FIX L1 filter: metrics-collector skip provider tanpa status_url + bukan di PING_URLS + bukan fonnte (healthchecks.io tidak masuk loop ping). Arsip: 3 file.** |
| **9 Juli 2026** | **S#342** | **Snapshot `sesi-342-m8-audit-trail` dibuat. M8 Audit Trail: arsip pra-integrasi writeMonitoringAudit(). File yang akan diubah: alert-lifecycle.service.ts (ACKNOWLEDGE/RESOLVE/REOPEN/AUTO_RESOLVED), alert-rules.repository.ts (RULE_UPDATE), alert-test/route.ts (TEST_ALERT). Arsip: 3 file.** |
| **10 Juli 2026** | **S#343** | **Snapshot `sesi-343-m9-guardrail` dibuat. M9 Guardrail Aksi Destruktif: arsip pra-M9. File yang akan diubah: alert-rules.repository.ts (tambah RULE_DISABLE/ENABLE + bulkDisableStaleRules), monitoring.service.ts (expose bulkDisableStaleRules), AlertRulesPanel.tsx (tombol Bersihkan Usang + modal), AlertRuleCard.tsx (modal konfirmasi toggle OFF). Baru: bulk-disable/route.ts. Arsip: 4 file.** |
| **10 Juli 2026** | **S#345** | **Snapshot `sesi-345-b2-severity` dibuat. B2 Severity Logic: arsip alert.service.ts pra-integrasi filter quiet hours berbasis severity. evaluateRule() akan ditambah blok cek severity CRITICAL vs WARNING/INFO + isQuietHour() helper. Arsip: 1 file (alert.service.ts).** |
| **10 Juli 2026** | **S#347-split** | **Snapshot `sesi-347-fix-b2-multi-recipient/lib/services/alert.service.pre-split.ts` dibuat. ATURAN 9: alert.service.ts 11.4 KB > 10 KB — arsip pra-split menjadi alert.service.ts + alert-helpers.service.ts. Arsip: 1 file.** |
| **10 Juli 2026** | **S#349** | **Snapshot `sesi-349-b3-business-impact` dibuat. B3 Dampak Bisnis: provider_instances +business_impact (text NULL) — SA isi teks dampak bisnis per instance. Tampil di badge DOWN/DEGRADED (L1, SystemBadgeGrid) dan AlertLogTable (L5). DDL migration + RPC fn_get_latest_metrics_per_provider DROP+CREATE (tambah business_impact). 14 file diubah: provider.types.ts, monitoring.types.ts, credential.repository.ts, alert-log.repository.ts, monitoring.service.ts, credential.service.ts, DialogTambahInstance.tsx, DialogKonfigurasi.body.tsx, DialogKonfigurasiKoneksi.tsx, API route PATCH instanceId, SystemBadgeGrid.tsx, UptimeSummaryTable.tsx, monitoring/page.tsx, monitoring/status/page.tsx. Arsip: 2 file types di sesi-349. Build PASS S#349.** |
| **10 Juli 2026** | **S#354** | **Snapshot `sesi-354-p0-1-insert-alert-log` dibuat. P0-1 (audit S#353): `insertAlertLog()` membuang `status` + `dedup_key` dari `.insert()` — 3 pemanggil di alert.service.ts mengirim keduanya, keduanya dibuang diam-diam. Akibat: dedup_key selalu NULL, status selalu default 'TRIGGERED'. Fitur mati: M3 Deduplication, A2 Auto-Resolve; log M4 + B2 berbohong. Grep LL#14 mengunci: `.insert()` ke alert_log hanya di satu titik. Arsip: 1 file (alert-log.repository.ts, 11.377 bytes, verifikasi byte-identik). Hutang tercatat: HUTANG-SPLIT-ALERTLOG-REPO (file 11.377 B > batas 10 KB ATURAN 9).** |
| **11 Juli 2026** | **S#355** | **Snapshot `sesi-355-m6-drain-decode` dibuat. Bug M6 drain terbongkar SETELAH Redis hidup (log cron dpl_EuQyj: `[alert-queue:wa/email] Item tidak valid (JSON parse error)`). Root cause: enqueue simpan `JSON.stringify(item)` -> `@upstash/redis` auto-deserialize saat lpop kembalikan object -> drain `JSON.parse(object)` gagal -> setiap item dibuang -> WA/Email tak terkirim (`alert_log.sent_via_wa=false, error_wa=null`, menggantung). M6 sebenarnya tak pernah mengirim (tertutup Redis mati sebelumnya). Fix: serahkan serialisasi ke klien -> enqueue push object (drop JSON.stringify) + drain pakai `lpop<T>()` langsung (drop JSON.parse) + guard field wajib. 4 titik di alert-queue.service.ts. Arsip: 1 file. Catatan: email recipient masih '0' (T-1, isu terpisah P1-3); pesan tampil id bukan nama provider (isu interpolasi terpisah).** |
| **11 Juli 2026** | **S#356** | **Snapshot `sesi-356-e1-provider-name` dibuat. P0-EMAIL E-1: pesan alert menampilkan UUID sebagai `provider_name`, bukan nama asli. 2 titik interpolasi (`buildAlertMessage()` di alert-helpers.service.ts + blok B2 quiet-hours di alert.service.ts). Fix: `findRulesByProvider()` tambah JOIN `service_providers(nama)` -> `rule.provider_nama` (nol query tambahan, pola sama findAllAlertRules). `buildAlertMessage` param `providerId`->`providerNama` (formatter murni). evaluateRule pakai `rule.provider_nama` di 2 titik. Caller `buildAlertMessage` = hanya alert.service.ts (LL#14 ter-trace). Arsip: 3 file (alert-rules.repository.ts, alert.service.ts, alert-helpers.service.ts).** |
| **11 Juli 2026** | **S#356** | **Snapshot `sesi-356-e2-success-flag` dibuat. P0-EMAIL E-2 (HUTANG-M6-SUCCESS-FLAG): jalur SUKSES `drainWAQueue`/`drainEmailQueue` tidak menandai `sent_via_wa`/`sent_via_email=true` — hanya jalur GAGAL yang menulis (error). Akibat: `alert_log` under-report, sukses tampak false ("log berbohong"). Fix: `updateAlertLogNotifResult()` payload dilebarkan terima `sent_via_wa`/`sent_via_email` boolean (body `.update(payload)` tak berubah). Tambah helper `recordWASuccess`/`recordEmailSuccess` (simetris dgn recordError, try/catch tak gagalkan drain), dipanggil di cabang sukses. Kolom sukses & error terpisah -> partial success (1 dari N penerima) tercatat jujur. Arsip: 2 file (alert-log.repository.ts, alert-queue.service.ts).** |
| **12 Juli 2026** | **S#359** | **Snapshot `sesi-359-drainqueues-after` dibuat. FIX AKAR smoke test 8B (WA sebagian tak terkirim, nomor gagal berpindah tiap run, "fetch failed" acak; email selalu masuk): `collectL1Metrics` memanggil `drainQueues()` + `pingHeartbeat()` fire-and-forget TELANJANG (tanpa await, tanpa `after()`). Di serverless Vercel, container di-freeze setelah HTTP response terkirim -> drain WA yang lambat (sleep 2s/nomor) keputus di tengah -> fetch in-flight ke Fonnte = "fetch failed", sisa item tak sempat terkirim. Email drain cepat (~500ms x2, paralel) -> selesai sebelum freeze -> itu sebab email selalu masuk. Fix (pola sama session-log S#058): bungkus drainQueues + pingHeartbeat dengan `after()` dari next/server (metrics-collector.service.ts) supaya platform jaga container hidup via waitUntil sampai drain tuntas; + `export const maxDuration = 60` di collect-metrics/route.ts (default Hobby 10s tak cukup untuk collect ~6s + drain ~8s; Hobby kini dukung 60s, fluid compute default-on). Hardening sekunder DITUNDA (retry WA + sendFonnteWA baca body Fonnte). Arsip: 2 file (metrics-collector.service.ts, collect-metrics/route.ts). Build PENDING verifikasi Philips.** |
| **14 Juli 2026** | **S#370** | **Snapshot `sesi-370-fix-monitoring-status-embed` dibuat. FIX halaman M01 Status gagal load ("Gagal memuat data monitoring status"). Akar: `findRecentAlertLogsWithImpact()` (alert-log.repository.ts, dari B3 S#349) embed `provider_instances` LANGSUNG dari `alert_log` — tak ada FK alert_log->provider_instances (FK hanya ke service_providers/alert_rules/users) -> PostgREST PGRST200 "could not find relationship" -> throw -> Promise.all di page.tsx M01 status gagal -> catch fallback. BUKAN akibat F0; bug laten S#349 baru kebuka (halaman belum pernah dibuka). Diagnosis via reproduksi: RPC fn_get_latest_metrics_per_provider OK, embed service_providers OK, hanya embed provider_instances jatuh + cek pg_constraint (FK tak ada). Fix: embed BERSARANG `service_providers(nama, provider_instances(business_impact,is_default))` — jalur FK valid kedua sisi; mapping baca pis dari `sp?.provider_instances`. Nol perubahan DB. Arsip: 1 file (alert-log.repository.ts, byte-identik pra-edit). Build/deploy + verifikasi runtime (reload halaman) PENDING Philips.** |
| **15 Juli 2026** | **S#373** | **Snapshot `sesi-373-fix-m2-autodisable` dibuat. BUG-038 + BUG-039 — dua pengaman anti-spam yang tak pernah bekerja, dibongkar saat TC-2 F0 dieksekusi (bukan dari baca kode). BUG-038 (M2 auto-disable, MATI SENYAP sejak S#332): `autoDisableRulesWithoutInstances()` pakai `.not('provider_id','in','(select distinct provider_id from provider_instances where is_aktif = true)')` — PostgREST TIDAK mendukung subquery di filter; string diperlakukan daftar nilai literal lalu di-cast uuid. Bukti SQL live: `SELECT 'select distinct ...'::uuid` -> ERROR 22P02 invalid input syntax for type uuid. Diperparah pemanggil di metrics-collector.service.ts: `try { await autoDisableRulesWithoutInstances() } catch { /* non-critical */ }` — catch KOSONG, nol log -> gagal tiap menit tanpa jejak; itu sebab TC-2 selalu tampak '0 kandidat'. Bukti runtime: instance github `is_aktif=false` 02:43 UTC -> 3 siklus cron (02:44/45/46, provider_metrics terverifikasi) -> 2 rule github TETAP `is_active=true`, `disabled_by=null`. BUKAN cacat F0 — F0 justru terbukti bekerja (flag `auto_disable_check_enabled` terbaca; kalau namespace rusak fungsi `return 0` di baris pertama). BUG-039 (M9 'Bersihkan Aturan Usang', belum pernah ditekan sejak S#343): `bulkDisableStaleRules()` pakai `service_providers!inner(is_active)` + `.eq('service_providers.is_active', false)` — kolom `is_active` TIDAK ADA di service_providers (aslinya `is_aktif`; verifikasi information_schema: alert_rules.is_active ADA, service_providers.is_aktif ADA, service_providers.is_active TIDAK ADA). Dua konvensi bahasa di satu domain -> tertukar; error TIDAK senyap (route 500). Audit sistemik LL#14 (20 file: seluruh klaster alert/monitoring + 12 repository lain): pola subquery-di-filter NIHIL kembaran (pola benar 2-query justru sudah standar project, mis. membershipRepo_findAll); `is_active`/`is_aktif` tertukar hanya 1 kejadian. Blast radius: autoDisableRulesWithoutInstances = 1 pemanggil (metrics-collector); bulkDisableStaleRules = monitoring.service -> bulk-disable route -> tombol AlertRulesPanel. Q-05 cr_function_deps: 0 baris (tak ada rekaman relasi pemanggil). KOREKSI dalam sesi: Q-05 membaca `cr_function_deps` (peta relasi), BUKAN katalog `cr_functions` — klaim awal 'kedua fungsi belum terdaftar' SALAH. Fakta terverifikasi: `bulkDisableStaleRules` SUDAH terdaftar (REPOSITORY/TRACKING/monitoring); `autoDisableRulesWithoutInstances` BELUM → didaftarkan S#373 (func_id 38a16076-cee0-4680-b11a-93724ef87247, SERVICE/TRACKING/monitoring, is_shared=false, kegunaan terisi — ATURAN 19/20). Nol perubahan Supabase -> ATURAN 27 tidak terpicu. Arsip: 3 file, byte-exact terverifikasi (alert-rules.service.ts 4.656 B, alert-rules.repository.ts 10.473 B, metrics-collector.service.ts 13.758 B). Hutang dicatat: HUTANG-SILENT-CATCH (catch kosong = penyakit sistemik, 3 korban: BUG-034 Edge Function, BUG-038 M2, TEMUAN-1 S#354 Redis), HUTANG-AUDIT-REPO (15 repository sisa), HUTANG-KOLOM-KONVENSI (is_aktif vs is_active). Task_Flow: FIX-BUG-M2-AUTODISABLE_sesi373.md (instance TEMPLATE_FIX_BUG). Fix + build + retest TC-2 runtime PENDING.** |
| **15 Juli 2026** | **S#374** | **Snapshot `sesi-374-instrumentasi-drain-wa` dibuat. INSTR-TC3 (alat ukur TC-3 F0) + pemecahan file. MASALAH: jeda antar-WA (config `fonnte_delay_seconds`) TIDAK BISA dibuktikan dengan alat yang ada — `alert_log` tak simpan waktu per penerima (1 alert = 1 baris, `sent_via_wa` satu kolom untuk semua nomor), `updated_at` tak pernah terisi (pg_trigger kosong, kode juga tak menulis), jalur SUKSES drain tak mencatat apa pun ke log Vercel (24 jam terakhir: nol baris), dan jam terima WA hanya presisi menit vs jeda uji 5 detik. Menerima 'kodenya kelihatan benar' sebagai bukti = pola yang menyembunyikan BUG-038 berbulan-bulan. TEMUAN SAAT EKSEKUSI (ATURAN 15): `alert-queue.service.ts` = 10.020 B (9,79 KB) — hanya 220 B dari batas kode 10 KB (keputusan arsitektur S#055+#061); instrumentasi (±550 B) TIDAK MUAT. Opsi 'helper kecil' ditolak Philips+Claude (sisa 50 B = masalah balik lagi sesi depan). DIPILIH Opsi B: pecah by kategori (ATURAN 31), pola sama S#347 (alert-helpers) + S#295 (collectors/). PINDAH MURNI ke `lib/services/alert-queue-drain.service.ts` (BARU): drainQueues + drainWAQueue + drainEmailQueue + recordWA/EmailError + recordWA/EmailSuccess + sleep — logika, urutan, penanganan error NOL berubah. `alert-queue.service.ts` kini sisi TULIS saja (enqueueWA/enqueueEmail + tipe + key Redis): 10.020 B -> 4.261 B. QUEUE_KEY_WA/QUEUE_KEY_EMAIL diekspor (satu sumber kebenaran, ATURAN 19 — dilarang tulis ulang string key di file lain). Tak ada impor melingkar (drain -> queue satu arah). INSTRUMENTASI (WA saja, scope TC-3): 1 baris `console.log('[alert-queue:wa] drain-selesai ' + JSON.stringify({sukses, gagal, config_fonnte_delay_seconds, delay_terpakai_ms, jeda_nyata_antar_wa_ms[], total_drain_ms}))` di akhir drainWAQueue; JSON 1-baris karena `get_runtime_logs` MCP hanya kembalikan console.log PERTAMA per request; diam total bila queue kosong (tak menambah kebisingan log cron/menit). `jeda_nyata` diukur antar AWAL pengiriman berturut-turut = durasi Fonnte sebelumnya + sleep(delay), jadi selalu >= delay. Drain Email BELUM diinstrumentasi (menyusul TC-4). KOREKSI DIRI dalam sesi: file drain versi pertama 10.199 B (9,96 KB, sisa 41 B) — header komentar kepanjangan; dirampingkan jadi 9.551 B (9,33 KB, sisa 689 B) sebelum lanjut. Pemakai `drainQueues` diverifikasi baca-file (registry `dipakai_oleh` KOSONG untuk semua fungsi = hutang data): HANYA metrics-collector.service.ts (1 baris import diubah ke file drain; pemanggilan di dalam after() + maxDuration=60 tak disentuh). alert.service.ts hanya impor enqueueWA/enqueueEmail (tetap valid); alert-test/route.ts kirim langsung (tak lewat queue); collect-metrics/route.ts hanya impor collectL1/L3Metrics. Nol perubahan Supabase -> ATURAN 27 tidak terpicu. Nol perubahan UI -> LL#9 tidak terpicu. cr_functions: `drainQueues` file_path diupdate ke file drain. Arsip: 2 file byte-exact terverifikasi (alert-queue.service.ts 10.020 B, metrics-collector.service.ts 14.275 B). Hutang dicatat: HUTANG-INSTR-EMAIL (instrumentasi drain email, TC-4), HUTANG-PECAH-KANAL-DRAIN (bila file drain mendekati 10 KB lagi -> pecah drain-wa/drain-email, jangan tambal), HUTANG-REGISTRY-DIPAKAI-OLEH (`dipakai_oleh` kosong -> peta pemakai tak bisa diandalkan, terpaksa verifikasi manual). Build + deploy Preview + eksekusi TC-3 PENDING.** |
| **15 Juli 2026** | **S#375** | **Snapshot `sesi-375-pecah-kanal-drain` dibuat. HUTANG-PECAH-KANAL-DRAIN (prasyarat TC-4, urutan disetujui Philips S#374): pecah kanal drain SEBELUM instrumentasi email. ALASAN URUTAN (pencegah kerja mundur, bukan kerapian): `alert-queue-drain.service.ts` = 9.551 B, sisa 689 B dari batas kode 10 KB (S#055+#061); instrumentasi drain email ±550 B -> ±10.100 B = sisa ~100 B. Langkah #3 PETA (HUTANG-SILENT-CATCH) ber-scope jalur tak-tertonton (cron/`after()`/background) dan `drainQueues` jalan persis di sana -> langkah #3 DIJAMIN membuka file ini dan terpaksa memecahnya duluan = kode dipindah DUA kali. Dikerjakan sekarang = menunggangi TC-4 yang memang harus buka file itu = sekali pindah. PINDAH MURNI (logika, urutan, penanganan error NOL berubah): `alert-queue-drain-wa.service.ts` BARU 6.080 B (drainWAQueue + INSTR-TC3 + recordWASuccess/recordWAError); `alert-queue-drain-email.service.ts` BARU 5.222 B (drainEmailQueue + retry backoff + recordEmailSuccess/recordEmailError; ruang cukup untuk instrumentasi email berikutnya); `alert-queue-drain.service.ts` 9.551 B -> 2.056 B, kini ORKESTRATOR saja (drainQueues = Promise.allSettled 2 kanal). File orkestrator SENGAJA tetap di path lama: pemanggil metrics-collector.service.ts + registry `cr_functions.drainQueues.file_path` menunjuk ke sana -> nol perubahan caller, nol kerja mundur (metrics-collector TIDAK disentuh sama sekali). `sleep()` (dipakai KEDUA kanal): ini skenario ATURAN 17.2.A — logic SUDAH ADA di kode (helper privat di file drain) tapi BELUM terdaftar di registry; jadi PROMOSI + registrasi, bukan fungsi baru. Bukti cek sebelum putus: Q-01 (sleep/delay/wait/pause) + Q-02 (fuzzy sleep/delay/backoff/wait) + Q-06 (kegunaan jeda/tunggu/delay) di `cr_functions` = NIHIL; codebase disisir (12 file lib/utils + fetch.server + fonnte.server + resend.server + provider-tester + metrics-collector) = tak ada implementasi sleep lain. Rumah: `lib/utils/async.utils.ts` BARU 1.754 B — utils TIDAK PERNAH impor services -> arah impor satu arah, nol risiko melingkar. Alternatif DITOLAK: (a) sleep di orkestrator -> melingkar (orkestrator->kanal->orkestrator); (b) salin ke 2 kanal -> duplikasi logic (ATURAN 11/19); (c) tumpang ke date.utils.ts -> beda kategori (itu hitung ISO timestamp untuk repository, bukan kontrol alur async). Registrasi mengikuti presedan `fetchWithTimeout`: cr_functions HELPER/CONFIG/infrastructure/is_shared=true + mirror Opsi A `SHARED_REGISTRY_CONFIG.md` (Domain infrastructure 3->4, RINGKASAN 20->21). Nol perubahan Supabase struktur -> ATURAN 27 tidak terpicu. Nol perubahan UI -> LL#9 tidak terpicu. Arsip: 1 file byte-exact via move_file (alert-queue-drain.service.ts 9.551 B) — bukan tulis-ulang dari ingatan. Build + registry + commit + deploy Preview + eksekusi TC-4 PENDING.** |
| **3 Agustus 2026** | **S#434** | **Snapshot `sesi-434-featurekey-policykey` dibuat. #68 TEMUAN-KATALOG-NILAI-TANPA-PENJAGA bagian b (perintah Philips). `metrics-collector.service.ts:209` mencocokkan item config lewat `i.feature_key === 'vercel_plan'` — KOLOM YANG SALAH. Makna yang benar: `feature_key` = nama GRUP, `policy_key` = nama ITEM. Baris itu selama ini bekerja HANYA karena `vercel_plan` kebetulan memakai namanya sendiri sebagai feature_key — 1 dari 38 baris yang begitu (9 grup menampung 101 baris; 38 baris sisanya jadi 'grup' beranggota satu). Diubah ke `i.policy_key === 'vercel_plan'`. Sapuan pemanggil config LENGKAP sebelum menyentuh apa pun: hanya 6 titik di seluruh kode (`platform_general` ×2 · `monitoring` · `security_login` · `sistem` · `getConfigItemsByKategori('Monitoring')`), dan hanya DUA yang bergantung pada feature_key-sebagai-item — yang kedua `monitoring/deep/page.tsx` (entri di `INDEX_ALERT_UI_DAN_KONFIGURASI`). Nol pemanggil memakai `loyalty_*` / `subscription_*` / `complaint_*` / `test_config` sebagai literal. Prasyarat data dibayar lebih dulu supaya tidak ada jendela rusak: `policy_key` NULL **20 → 0** (UPDATE, row-count tetap 139, ATURAN 27 tidak terpicu) — sesudah itu kode masih sah membaca feature_key, jadi urutannya aman. Arsip: 1 berkas byte-exact (metrics-collector.service.ts 14.454 B, SHA-256 `e6e60418c9dcbed16609…`). Build + commit PENDING verifikasi Philips.** |
